# 工作日志

当前状态（2026-09-18）：第 1–6 步实现与自动化验证完成；第 7 步代码、构建与自动化已完成；第 8–9 Part 正式实现与 Windows 自动验收完成，macOS 与实际窗口人工验收待继续。以下历史记录保留当时状态。

追加任务时的记录（2026-09-18）：主人已要求将后续方案写入 [任务总纲](00-M2.3任务总纲.md)，新增第 8 Part「文本排版资源化，统一测量与绘制复用」和第 9 Part「复用已发布绘制片段，实施可靠的可见性裁剪」，均为待实施。总纲已明确缓存/资源/发布边界、颜色变化不重新排版、普通 scroll 不承担 Lazy 语义、DrawOps 片段直接复用、必要验证及新会话交接要求；本次只追加施工任务，未修改生产代码或实施优化。

为了防止 AI Agent 丢失工作状态，导致重复执行工作。要求：做了什么，将做什么，必须及时更新入本日志空间。这相当于是 AI Agent 的外置记忆！日志是随时有进展就写的，不能受提醒了才写，也不要再在日志中写`受主人提醒`，这是 AI 的分内工作：不及时写日志导致开放进展颗粒度没对齐，吃亏的是 AI，未来辛苦翻工的也是 AI！

以下是正式条目：

## 1-3趴

- 和主人讨论，确立了当前总纲已包含拍板后的爆改顺序。目标仍是完整完成 1–3，未缩减。
- 核对工作区：`Modifier.cpp` 仍是错误的空 diff 实现；旧聚合链路仍在 Layout/Paint/HitTest/QuickJS/tests 中。此前“恢复正确”的声明不成立。现在直接替换该模型，不继续修补旧 diff。
- 本轮先建立类型化 descriptor → 原生实例链的唯一协调语义，再迁移测量/放置与绘制/命中。中间编译失败是明确的消费者迁移状态，不回退到聚合字段。
- 已删除 `Modifier.h` 中 `CompiledModifier`、独立 paint/layout 列表和聚合 input/transform/scroll 字段，替换为 `ModifierValue` variant、`ModifierDescriptor`、`ModifierInstance`、`ModifierChain`。实例记录自己及内层测量尺寸、偏移、几何；新增独立 Placement 失效位。此刻旧消费者尚未迁移，预期暂不可整体编译。
- 已替换 `Modifier.cpp` 错误空 diff：原生统一校验有限值、尺寸/范围、绘制参数与重复 key；协调支持有 key 元素移动、无 key 元素按类型及顺序匹配，返回退休 handle。不同类型替换产生新身份；typed 更新拒绝过期 handle 和改变元素类型。
- `ArrangeNode` 已改持有实例链，`SetModifierMutation` 只携带 descriptors；`LayoutTree` 已调用原生协调及精确失效分类。纯绘制值不产生测量失效；offset 与 scroll 值使用 Placement。原生布局/绘制/命中消费者继续迁移中。
- Layout 已改为直接遍历完整 Modifier 实例链；每层保留 outer/child measured size、child offset 与 bounds，节点另存 contentBounds。padding/required size/scroll 的包裹几何不再从最终单个 bounds 倒推；offset 只在所在层放置内层内容，graphicsLayer 不再伪装布局偏移。
- measure 和 place 已成为独立入口；放置复用测量结果。修正 fill 遇无界约束、required size 的约束外尺寸与居中、defaultMinSize 只在零最小约束生效等行为。父布局读取链中 parent-data 的明确归约结果，不读取旧聚合结构。
- 绘制已改为递归进入/退出完整实例链，背景在内容前、border/innerShadow 在内容后；clip 和重复 graphicsLayer 独立入栈出栈。每个操作使用所在实例 bounds，内容使用 contentBounds；JUCE transform 增加真实 translation，不再篡改布局坐标。
- 新增共享 ModifierGeometry：矩形/圆形/圆角裁剪检测、每层逆变换及进入规则，供命中和滚动使用。阴影沿用现有原生绘制能力（不是本期新造模糊阴影引擎）。输入文字矩形改为使用 contentBounds。
- 命中已迁移到完整实例链，每层按自身 bounds、逆变换与裁剪规则进入；内层 clickable 优先，外层交互区域保留。HitTestResult 携带具体 Modifier handle 和事件槽，pointer-up 校验同一实例/槽，防止同节点替换后的误点击。
- 滚动路由已改为收集链中具体滚动实例，支持同节点重复滚动层、变换/裁剪与内层到外层消费；viewport/content 来自该实例的 measured/childMeasured，不再从全节点子矩形猜测。
- QuickJS Modifier reader 已改为按原顺序解码 `ModifierDescriptors`；不再聚合 transform/input、维护副本 paint 链或合并 padding。原生统一校验整条描述后再登记回调，避免解析失败留下半条 callback 注册。回调注册表正在接入每个元素独立的资源身份。
- 回调注册表新增 Modifier callback 资源登记/保留/退休，资源 token 在进程中不复用；同函数的重复元素仍分配独立槽，槽不依赖数组下标。Modifier 回调不再写入节点单一 eventSlots[kind]。节点删除遍历实际注册记录释放全部回调，替换链释放不再引用的元素回调。
- Framework Modifier 增加 `keyed(key)`（给最后一层设协调 key）和 descriptor key，明确 key 不等于 native handle。第 2 步仍需把 callback 可见性/提交失败回滚收进统一帧边界。
- SceneFramePipeline 开始使用独立 measure/place 入口：Placement 不再触发 measure，绘制/命中随放置重建；full fallback 包含必要测量。尚未声称第 2 步完成：提交、真实命中快照和原子发布仍待重写。
- 新增持久 `arrange_modifier_runtime` 行为测试：keyed 重排与退休、代际拒绝、无效描述不半写入、背景/padding/layer 包裹、required size 溢出居中、圆形裁剪、offset 作用域、点击途中替换、颜色/位置/尺寸阶段选择。正在执行。
- `arrange_core` 和 `arrange_quickjs` 正式库构建通过；新 Modifier 行为测试实际执行通过（identity/geometry/painting/hit/phase）。这是局部证据，不代表 1–3 或第 1 步已验收。
- 旧 smoke 测试的 descriptor 构造/查找已迁移到新链；没有保留 CompiledModifier 兼容类型。其余原有行为断言保留，接下来执行并按实际语义审查差异。
- 真实现有 Demo 的 `build/demo-ui-dist/app.js` 已经通过新 QuickJS/core 链加载与布局，smoke 输出 typed mutations=87；尚未验证完整 GUI 交互。
- 新增 QuickJS→SceneFramePipeline 持久测试，覆盖重复 clickable 的独立 callback/handle、变换内外层命中、真实调用、链替换后的 callback 回收、非法描述不会泄漏回调以及 context reset 拒绝旧 token。执行中。
- QuickJS Modifier 行为测试通过；arrange_juce 库也构建通过（仅 JUCE 在当前 macOS SDK 的上游 deprecated 警告）。
- 继续审查发现同一次提交内 callback 先注册后退休会被旧两列表应用顺序复活：已修正注册表和 MutationTransaction 合批，保留每个槽的最终注册/退休状态，并加入实际 QuickJS 同批次注册后移除的回归场景。
- Modifier 可选 numeric/bool 字段不再通过隐式 JS 转换吞错；直接检查 JSValue 类型，再由原生校验数值范围。transformOrigin 数值不在 reader 中偷偷 clamp，保留真实输入。
- 再次收紧事件身份：移除了刚接入时使用 path 前缀判断 Modifier 回调的实现，改为 EventSlotOwner 枚举 + resource/generation 数字身份，路径只保留既有诊断用途。事件 hash/相等比较包含 owner/resource/generation，退休与路由不依赖魔法字符串。
- 下一段施工入口：补齐第 1 步重复 padding/clip/graphicsLayer 与滚动的组合测试和真实 GUI 验收；随后第 2 步引入 typed host input/Modifier input binding、统一提交、node/context 代际、真实命中快照与原子发布。第 3 步尚未修改，范围不变。
- 现阶段边界：SceneFramePipeline 的 Apply/BuildHitTest/Publish 仍有旧实现，QuickJS 普通 node prop 回调的替换可见性也仍需随第 2 步统一；不得把本轮通过的局部测试当作完整任务完成。
- 本轮末复验：最新 typed EventSlotOwner/resource/generation 版本的 core/QuickJS 构建通过，两项新持久测试重新链接并执行通过（不是复用旧二进制）。测试手动使用当前 demo-ninja-debug-macos 生成的本项目静态库；目标已登记在 cpp_tests/CMakeLists.txt，后续正式 root CTest 构建统一运行。
- 当前 goal 保持 active：完成判据是总纲完整 1–3 的逐条验收，而不是本轮修改数量或上述局部通过结果。本轮属于实际代码与验证进展。
- 新增 HostInput 枚举、边界名称解析、输入相等判断和失效 schema；textStyle 只改 color 归 Paint，布局对齐归 Placement，资源/可访问性/交互输入各自分类，不把所有 prop 更新都算 Layout。
- 新增 NodeHandle、BindingHandle、四种 typed BindingTarget（host input / 具体 Modifier input / 整链表达式 / host event）、SlotValue、SlotUpdateBatch 和运行计数器。MutationTransaction 纳入 binding 注册/退休和值更新，合批保留其顺序；尚在接入 NativeScene 与 QuickJS，未宣称生产接通。
- LayoutTree 接入唯一的 typed host/Modifier setter，旧直接 mutation 入口也调用它；节点创建获得不复用 generation，并拒绝覆盖活节点。相等输入不标脏，host 名称统一规范化。
- NativeScene 接入 binding 注册/退休、节点/实例存活校验、按结构→生命周期→值更新执行、实际计数器；链输入退休实例后立即清理失效绑定，节点删除清理其绑定。错误类型抛错、过期更新拒绝并计数，不会误写新实例。
- NativeScene::apply 改为候选 scene 成功后提交，异常不保留半次结构或输入修改；SceneFramePipeline 即将复用候选执行入口，把布局/绘制失败也纳入同一发布边界。
- SceneFramePipeline 改为候选 scene/frame 执行：结构、slot、measure、place、DrawOps 任一异常都不覆盖先前成功发布的状态。成功后一起交换 scene 与 PublishedFrame，增加 revision 和真实 submissions/failures/measure/place/paint/publication 计数。
- 删除了原先只写日志却没有实际构建的 BuildHitTest“已运行”记录；下一项要建立实际命中快照再接回该阶段，hitBuilds 当前保持零，不伪造计数。
- QuickJS 生产 setText/setProp/setModifier 已改为注册持久 typed binding 并写入 SlotUpdateBatch；结构创建/插删仍在 tree mutations。现有 Framework 和 Demo 调用直接进入该通道，没有另外的演示专用提交接口。
- QuickJS 保存每个 node generation 与输入 binding，删除子树递归退休、context reset 清空。修复旧删除顺序先清 children 索引导致子孙类型/绑定无法递归释放的问题。
- 事件/动画调用入口不再先清空 pending transaction，避免同一帧多个来源丢失先前值更新；context 重建仍明确清空旧提交。
- 新增正式 CTest 目标 arrange_slot_runtime，验证同帧 create/register/update、具体 Modifier slot 的颜色/位置阶段、host textStyle 只改颜色不测量、相等值跳过、错误 payload 原子失败、退休实例/节点重建/reset 拒绝旧绑定以及直接 apply 结构失败不半写入。正在执行。
- arrange_slot_runtime 已通过。QuickJS app smoke 已从只取 treeMutations 改为应用完整 NativeScene submission，并检查实际 ModifierDescriptors slot；不继续用旧 smoke 丢掉 typed 更新后冒充验证。滚动 callback 入口同样移除清空 pending 的旧逻辑。
- BuildHitTest 现已实际构建不可变 HitTestSnapshot（命中区域 + 共享父约束索引），与 DrawOps 同时发布；没有复制另一套布局算法。保留快照时不重建，hitBuilds 只在实际构建处增加。
- JUCE 点击输入主链改为查询 PublishedFrame 的命中快照，不在 pointer 回调中重新遍历可变树。core 的按树调用仅通过同一个快照构建/查询实现供直接使用；绘制和点击输入现在消费同一次成功发布的结果。
- 已删除 MutationTransaction 的分类数组，改为唯一有序 SubmissionOperation 流。NativeScene 按产生顺序执行结构、binding 注册/退休、typed 输入和事件资源操作，合批只串接，不再把跨批次创建/删除和值更新重新排序。QuickJS 写入、回调登记及 JUCE 文本输入生产者同步迁移；相关测试消费者正在迁移。
- 继续按本总纲完整 1–3 施工；上一轮重新索要拍板属于错误，不存在待确认前提。
- 已迁移现有 core/Modifier/slot/QuickJS smoke 的提交构造和检查，删除对旧分类数组的依赖。unmount 调整为先解绑/退休回调再删树，遵守有序流，不在已删除节点上清事件属性。原生库构建中，下一项加入跨提交合批顺序与命中快照一致性的行为验证。
- 新增有序提交行为验证：跨批次先写后退休必须保留有效写入、退休后的迟到写拒绝；先错误写后删除不能靠重排掩盖错误；同 ID 删除重建拒绝旧代际。命中快照验证补齐颜色更新保留、放置更新重建、失败发布保留上次快照。
- 回调资源改为不可变 token：node 事件替换也获得新资源身份，不提前覆盖旧回调。QuickJS 仅允许调用成功发布集合中的 token，成功发布后按原生真实存活集合回收资源；替换了不带发布上下文的 flushRetired 接口。ArrangeRuntime 队列分发先核验已提交场景中的 token。
- 普通 node event 已从 SetEventSlot 旁路迁入持久 EventInputTarget binding；删除节点、unmount 和 context reset 同步释放事件 binding。失败后旧已发布集合保持不变；JS 结构账本的失败重同步仍待接齐，不把此项改动视为全失败恢复完成。
- 新增真实 QuickJS 回调发布行为验证：首次未发布不可调用、node 事件替换生成新 token、故障帧保留旧闭包并隔离新闭包、成功发布回收旧资源并启用新闭包。开始配置独立的根项目 CTest 构建（本项目 FetchContent，固定依赖版本），后续以正式目标运行测试。
- 修复运行时失败后直接丢弃提交造成 JS/native 账本分叉的问题：ScenePipelineState 保存失败提交，下一次实际工作请求先重放它，再顺序合并新提交；失败本身不驱动自动重试循环。成功才确认回调发布；reset/明确清队列会清除失败提交。永久非法结构仍会报错并需要重建 context，不能宣称回滚任意 JS 副作用。
- 有序 slot/命中快照行为测试已实际通过；新版 QuickJS 回调发布测试及真实 Demo bundle smoke 均通过（有序操作 143 条）。正式根项目配置仍在构建 JUCE 的 juceaide。
- 新增正式 arrange_frame_submission 测试：注入真实 TextMeasurer 失败，确认候选节点与命中不发布；恢复测量后同一创建提交先重放，再应用后续值，绑定不丢失；reset 丢弃旧 context 的故障提交。修复 CTest 的 QuickJS app smoke 未传真实 bundle 路径的问题。
- 删除 EditorTimerDriver、ArrangeEditor 的 Timer 继承及 timerCallback、60/20Hz 视觉频率策略；生产视觉时钟只由 JUCE VBlankAttachment 提供，无 peer 时等待真实 VBlank。configure/reload/resize 不再同步跑画面，dev reload 在该帧求值前执行。
- 新增 VBlankSource / ManualVBlankSource / VBlankFrameDriver，生产和手动源共用时间戳校验、重复 tick 抑制与不可重入分发入口。EditorFrameClock 保留回调期间延后停钟，避免释放正在执行的 JUCE attachment。单帧内部诊断二次 pump 与 overlay 发布仍在后续收束范围。
- 删除 FramePumpDriver 因诊断变化再次 pumpFrame 的路径，避免同一次 VBlank 采样动画两次。删除 ScenePipelineState 依据 pointer/wheel/key/text/IME/animation 名称强行标脏的逻辑；失效来自真实输入更新，resize 和资源完成保留各自语义。旧 smoke 的意图即失效断言已改为无值变化不重建。
- 新增 ManualVBlankSource → 同一 VBlankFrameDriver → ArrangeRuntime → QuickJS 动画 → 原生发布的行为测试，验证一次采样、下一帧续订、重复/倒退/NaN 时间戳与回调重入抑制、停钟和恢复。删除已无消费者的分类 BindingMutation/SlotUpdateBatch 容器。
- 根项目 CTest 首轮 6/7 通过，core smoke 暴露旧测试把“改颜色同时移除 clickable”当成纯颜色更新。已改为保持实例链结构只改 background 色值，并让失败打印具体用例和错误码；未知 host 输入的异常断言迁移到类型化校验的 invalid_argument。未放宽阶段跳过要求。
- core smoke 的无工作检查改为验证流水线未运行、revision 和 hit snapshot 不变；不再把上次已发布帧的历史 invalidation 当成本次执行结果。QuickJS pending job 稳定化加入预算，防止自我续订的微任务让原生帧永久阻塞；正在接通同时间戳共享预算及验证。
- QuickJS 同一帧时间戳内所有事件与动画共享 pending-job 预算，新帧才补充；加入真实无限 Promise 续订脚本，要求返回明确“不稳定”错误而非挂死。
- 最新根项目正式构建与 CTest 已通过全部 7 项：core smoke、真实 Demo QuickJS、JUCE runtime、Modifier、QuickJS 回调/微任务、typed slot、有序帧失败恢复/ManualVBlank。该证据覆盖当前原生修改，不代表第 1–3 步全部完成。正在重建真实 Standalone，验证移除 Timer fallback 后的实际窗口首帧和交互。
- 重建 Standalone 后真实首帧显示成功，系统采样确认正在经过 JUCE VBlankAttachment → VBlankFrameDriver → 正式流水线。GUI 点击/文字操作尚未看到更新，不能据测试通过判定 GUI 验收；已加入临时 pointer 坐标/命中追踪定位，定位后移除。
- GUI 问题继续定位：后台坐标点击未产生已放在 interactive 检查之后的追踪记录；将临时追踪前移到输入入口，以区分投递失败与 session 被判不可交互。另审查发现 repaint 的诊断/overlay 早返回可能遮蔽同帧主画面更新，后续按实际证据修正。
- 修复 repaint 的分支遮蔽：主画面绘制构建后必定请求场景重绘，不能被同帧 diagnostics/overlay 标志提前返回吞掉；只有附属层变化时合并两个区域。当前整场景 DrawOps 构建对应整场景 repaint，正确覆盖旧位置、graphicsLayer 和 required size 溢出；局部投影区域缓存属于第 4 步。该缺陷由源码明确证实，尚未把它认定为后台 GUI 操作无变化的唯一原因。
- 临时追踪前移后仍无 pointer 入口记录，说明本次后台 CUA 坐标操作未投递到 Arrange 输入处理；真实窗口首帧及 VBlank 执行已确认，实际 GUI 点击/键盘仍未验收，不能用截图冒充成功。已移除全部临时文件追踪代码。
- 正式 JUCE Demo smoke 的 13 次点击改为从 PublishedFrame 命中快照取得区域，经过 PointerInputProcessor down/up 才入队 JS 回调，避免仅直接调用 callback 绕过命中主链。
- 第 1 步补齐组合行为验证：同节点双 padding、双 clip、双 graphicsLayer 的独立层几何与逆变换命中；同节点两个有独立 viewport 的滚动实例，内层优先消费、到边界后外层消费、两层位移叠加与外层裁剪。
- 本轮最终复验：最新根项目 CTest 7/7 全通过（3.23s），包含新重复 Modifier/嵌套滚动、实际 PublishedFrame pointer down/up 的 Demo 点击、故障测量重放、ManualVBlank 和无限微任务退出。旧分类提交字段、flushRetired 接口、Timer fallback 和临时 pointer trace 源码已无残留。正在将最终源码重新链接回 Standalone，避免留带临时追踪的可执行文件。
- 第 1 步尚未通过完整退出审计：变换后的文本输入/焦点坐标与生命周期仍须收齐；真实 GUI 首帧已见，后台自动化未送达 pointer 入口，GUI 连续点击/输入/滚动不能据此声称验收。重复链和嵌套滚动现已有行为覆盖。
- 第 2 步继续收齐：向 JS 暴露正式 binding 注册/更新/释放及所有权；scene/PublishedFrame 与 overlay/diagnostics/资源发布统一（当前附属层仍在正式 scene 发布后写入）；JS 执行异常、永久非法提交、reload/context 重建的完整恢复边界。失败 native 提交现在保留待重放，不能再退回“失败清队列继续跑”。同帧多次 wheel 在旧已发布 scroll value 上计算可能丢累加，需要验证并解决。
- 第 3 步仍未实现结构执行域/持久值绑定与组件边界改造，必须继续 runtime-core + compiler-arrange + props/slots + SFC/Vite/Demo 一起施工。现有 createRenderer、DOM 编译假设与 Web 宿主模拟不算 Arrange Vue 交付。
- 不把本轮原生库/7 项测试/首帧成功视为任何整步完成，不修改任务范围，不需要再次拍板。
- 最终 Standalone 已重新链接成功，包含 repaint 修正并移除临时 pointer 追踪；本轮启动的测试进程已关闭。
- QuickJS 新增 registerBinding(nodeId, inputName)、updateBinding(handle, value)、releaseBinding(handle)：输入名称仅在注册边界解析成 HostInput/ModifierChain/EventInput，更新使用 bigint identity/generation 句柄和直接 JSValue 解码。过期/跨 context 句柄在解码前拒绝；同一输入重新注册退休前任，node 删除/unmount/reset 清理账本。尚在迁移 Framework 正式消费者，不以接口存在认定本步完成。
- Arrange Vue runtime-core 新增 ValueExpression/ValueBinding：独立 ReactiveEffect 追踪普通表达式，组件结构任务优先，值任务按 owner 排序，销毁停止订阅和已排队任务；结构协调复用绑定并更换闭包，避免 keyed item/index 陈旧。renderer 的 host 和 component props 共用绑定入口，组件值写入 updateProps 而不强制 instance.update。
- compiler-core 开始把静态键动态 props/事件编译为 arrangeValue(() => 原表达式)，普通 Ref/computed/helper 在实际值执行域读取。当前在接通和验证通用表达式路径；动态 spread、插值、slots/defaults/attrs 与 HMR 等还未完成，不能把第一批 transform 当成第 3 步交付。
- Framework host 现已正式使用 register/update/release binding，文本、props、Modifier 和事件值不再通过隐式 setX 生产入口；节点存储输入句柄，移除子树先释放 binding。native target 必须提供正式绑定能力，不提供旧 setX 兜底。
- 值执行域的写入阶段暂停依赖追踪，避免原生提交或组件 props setter 的内部读取污染表达式依赖。正在执行编译器/运行时/QuickJS 联合验证和迁移测试 target。
- renderer 持久测试的 native target 已迁移到显式 binding 协议，通过注册表记录实际值更新，保留原有外部行为断言。下一项验证跨组件计算值只更新 binding、不重跑结构，并验证分支删除取消待执行值任务。
- 首批运行时验证实际通过：跨组件 props/computed 值更新父子结构均不重跑；模板普通 TS helper 表达式由编译器放入值执行域；同帧分支删除取消排队绑定并释放 native 句柄。新增 keyed 重排复用句柄、更换 item/index 闭包及条件依赖退订验证；正式 SFC Demo 构建进行中。
- 实际 SFC Demo 暴露对象字面量被输出成箭头函数语句块的编译错误；已对绑定 getter 的返回表达式统一加括号，保留对象、条件与复合表达式的值语义。继续以真实 bundle 验证，不将之前的局部测试当作生产编译成功。
- 编译修复进一步使用 AST 原有显式 return 函数体，避免把任意 JS 节点强塞进文本 compound 类型。真实 Demo bundle 已产出，重新链接后的 QuickJS 挂载通过；JUCE smoke 的文案断言仍指向旧 `Clicks`，已跟随当前 Demo 的 `撅` 文案更新，点击次数要求保持 13。删除仍引用已迁入 old-src 的旧 CLI normalizeViteArgs 测试；当前 Vite 行为测试保留。
- compiler-arrange 正式入口已替换为原生目标编译：原厂元素识别、core v-model 输入/更新回调，不再加载 DOM event/v-model/style/HTML 静态串/Transition 变换。class/style、Web 指令和浏览器事件修饰符在源位置报错；不再静默忽略。接下来删除 Framework 浏览器对象模拟，并完善结构锚点/根替换。
- Framework 删除 Document/ShadowRoot、tagName、addEventListener/dispatchArrangeEvent 等浏览器模拟；文本输入改由原生 modelValue binding/typed callback 传递。新增稳定 Root（ID 1）承接所有顶层节点，根节点不再占用时钟/原生宿主固定根身份。注释和空 Fragment 锚点只保留在 JS 顺序树，不进入布局；根替换、重排、子树文本替换均提交真实创建/移动/解绑/删除操作，删除了 invalidate 冒充文本结构变更的路径。正在迁移相关断言并验证。
- 值执行域现在归属组件 effect scope；scope 停止会取消排队任务，分支解绑同时从活 scope 中移除 effect，避免长期组件反复切分支积累停用 effect。VNode 保留表达式来源，clone 后继续复用/建立绑定，不因第一次解析 props 丢失闭包。普通单文本插值和混合文本节点已接入独立值执行域，正在验证 native Text 与 Fragment 的行为。
- 稳定 Root/JS 锚点与原生 compiler 新入口已接通真实 Demo：QuickJS App smoke 和 JUCE runtime smoke 均通过，包含 13 次 PublishedFrame 命中点击、滚动快照与 Input submit。
- 值绑定调度改为脏时执行；结构协调刷新过闭包后，同队列中的旧值任务不会再次求值。attrs 改为浅反应式逐字段跟踪，默认 fallthrough 为绑定表达式，避免透传组件每次 attr 数值变化都重跑结构；正在增加多层透传与文本/分支组合回归。
- 增加真实编译结果执行的回归：对象字面量、文本插值与 native v-model、同一个 Ref 同时驱动结构和值、两层 attrs 透传、已挂载 VNode clone 后表达式存活，以及 Web 语义源码诊断。接下来按运行结果修复，不降低阶段/结构执行次数断言。
- 新测试定位了两处真实边界错误：Vue parser 无条件把大写原生标签判成组件，导致 Text 插值变成嵌套 Text；已让原生 schema 优先。组件 patch 期间的 attrs 账本读取污染了上层结构 effect；现将协调过程置于 pauseTracking，嵌套 render/value effect 自己建立实际读取域。clone 挂载的第一次值求值只填新 VNode，不提前写其复制来的旧宿主。
- 新增 arrangeProps 编译协同：动态 spread/动态键/对象事件表达式由 computed 记录值，键集合和 reserved key/ref 形成保持相等身份的结构投影，普通字段仍生成持久 ValueBinding。相同键集的值变化不触发结构重跑；键增删或 key/ref 变化才进入结构协调。移除 compiler-core props 的 class/style/HTML normalization 路径。
- 动态 prop 删除正式使用空 typed 值清除原生输入，NativeScene 按旧值→默认值计算失效后移除字段，重复清除不产生工作；未知输入仍拒绝。Framework 不再把 undefined 动态 prop 当作不支持值抛错。新增 tsx 开发依赖并更新 test 命令，替代 Node 26 已移除的 transform-types 选项，后续验证不依赖 /tmp bundler。
- 测试宿主改为验证真实树约束（先创建、父子顺序、移除前退休 binding），不再用空结构回调掩盖错误。新增编译后的 keyed Fragment/根替换/空列表回填、spread 数值阶段与 key 变化/字段删除、20 次分支切换后的 scope effect 回收验证。此前 16 项 runtime 测试已用正式 tsx 命令通过。
- scoped slot 参数现按实际读取延后：renderSlot 提供只读 getter，编译器将解构参数的引用改写为读取时解构，保留普通值、默认值/别名以及 TS helper 语义，不要求 Ref/getter DX。alias 记录作用域深度避免 v-for/函数局部同名误改写。嵌套 slot 的动态归属继续保留，正在添加行为验证。
- SFC/Vite 已改为拒绝 Web 标签/class/style 与 style blocks；移除原先“警告然后忽略”的字符串扫描及相应旧测试断言，保留原生模板的无警告验证。
- scoped slot 验证加入完整 Parent→Outlet props→slot 解构别名/default→普通 helper→Text，要求父与 Outlet render 均只执行一次；同时验证 slot 变量与内部 map 回调同名时不破坏词法作用域。
- 14 项 Arrange Vue 行为测试通过，scoped slot 的默认值、别名与内部同名 lambda 验证通过。新增实际结构执行/值求值/值写入/活跃值绑定计数器，计数直接放在 renderComponentRoot 与 ReactiveEffect 执行/退休入口；下一项将组合案例放进真实 SFC Demo，并用 QuickJS/native 帧阶段验证。
- 真实 Demo 计数器现经过 App → CounterPanel（computed + 普通 helper）→ ValueSurface（computed props）→ scoped slot 解构 → Text/Modifier；颜色与 offset 动画共同经过该路径。保留原文案与点击/输入/滚动功能，新增的不是孤立接口 demo。正在构建并复验 native 点击与动画帧。
- 新增正式 SFC→Vite→QuickJS→ArrangeRuntime 验证页面，直接复用真实 Demo 的 CounterPanel/ValueSurface。输入命令驱动颜色、位置、文本、分支和 keyed 列表；页面用实际 JS 计数验证纯值更新不重跑结构或泄漏绑定，原生测试再断言 measure/place/paint/hit 的真实执行计数。FrameExecutionCounters 已从 ArrangeRuntime 提供只读访问。
- 新增 CTest `arrange_vue_runtime` 并将 SFC fixture 构建登记为 CMake 依赖：验证真实组件/slot 链纯颜色只 paint、offset 不 measure、文本会 measure，Modifier handle 保留，12 轮颜色/位置无资源增长，keyed 原生顺序与 12 轮分支删除/恢复的回收。此时处于首次构建/执行，尚未记录通过。
- QuickJS 空值清除接通后同步收紧 reader：数组、函数、bigint/symbol、过深对象与非有限数字必须抛错，不能被解码成 null 后意外清除字段。Binding bigint 句柄增加无损 uint64 回验，拒绝负数、溢出和零，避免取模后撞到活句柄。
- 集成测试首轮缺少宿主加载阶段的 initial submission 入队，导致原生无场景；已按实际 RuntimePackageBinder/现有 Demo smoke 的加载边界，将挂载提交移交 ArrangeRuntime。未把该首轮失败归为 Vue 值域问题。38 项 runtime 测试已通过。
- 新增真实 QuickJS explicit binding 生命周期测试：重复注册退休旧 writer、旧更新拒绝、bigint 溢出/负数/number 句柄拒绝、错误 payload 不吞错、textStyle 清除、幂等释放、同 ID 删除重建后旧代际拒绝与最终活跃绑定数。
- 新完整 SFC/native 测试已通过：真实 JS 计数确认跨 props/computed/helper/slot 纯值更新不重跑结构；原生颜色只绘制、offset 仅放置/绘制/命中、文本测量、实例身份与分支/列表回收均满足断言。
- 删除组件协调“props 不同即直接 instance.update”的旧前提：先更新反应式 props/slots 和 pre watchers，再由实际结构 effect 脏状态或 slot 结构变化决定 render。补齐 fallthrough 本地/外部事件绑定的组合，数组回调转换为原生单个 typed callback。
- 值写入失败时不再提前缓存为成功值；只有 sink 完成后才确认 value/initialized 并计入写入数。新增普通手写 render props→子值消费者/default、子结构实际读取 props 与 pre/post watch 顺序，以及本地+fallthrough 事件组合验证。
- 文本输入几何已改为复用实际祖先/Modifier 路径：点击、拖选、IME 点索引先逆变换到 content 坐标，caret/文字范围正变换到根坐标。TextInputLayoutModel 修正残留的 node.bounds，改用 contentBounds。overlay 通过 DrawOpsBuilder 同一 Modifier 递归进入/退出逻辑包裹祖先 transform/clip/scroll/alpha，避免光标和选区浮在错误坐标。焦点记录 node generation，删除重建/disabled 后不能继续编辑旧会话；disabled 祖先从命中中排除。当前正在构建并添加变换输入行为验证。
- 滚轮在同一已发布 revision 内以具体 Modifier handle 累加待提交位置；后续 wheel 不再反复从同一旧 scrollValue 起算。新 revision/reset 清除预测值，未发布失败帧继续保留待提交累计；路由/夹取/内外层消费仍由原 ScrollDispatcher 决定，未提前修改已发布几何。disabled 祖先同时阻止滚动路由。
- 新增 JUCE 原生输入行为验证：两层祖先 transform + clip + padding 下点击 caret、IME caret 矩形与字符点索引往返、overlay 层级、disabled 祖先、同 ID 重建焦点拒绝；同 revision 两次 wheel 得到 48→96 且已发布树仍为 0，新 revision/reset 清除预测。
- 接续验证修正输入测试中不存在的 PaddingModifier 类型，改为正式 LayoutModifierSemantics/Padding 描述；生产模型没有增加第二种 padding 类型。
- 最新输入几何/滚轮、QuickJS 绑定生命周期与真实 SFC/native 三项测试均已通过。原生帧新增发布前 finalizer，完整比较 overlay（含 transform/clip）与诊断附件，统一 revision 和发布计数；删除 ScenePipelineState/ArrangeRuntime 直接改写已发布附件的接口。正在迁移 JUCE 正式消费者和失败诊断发布。
- JUCE 主链改为在候选 scene 上准备输入 overlay/焦点视口、诊断与图片资源，然后统一发布；输入会话复制准备、成功后才提交。被动 paint 接口只接受 PublishedFrame，删除读取 session/diagnostics/可变 scene 决定画面的路径。永久 JS/提交/资源错误停止该 context，保留最后几何并发布诊断；reload/reset 重建恢复。正在构建及补充发布失败行为验证。
- 新发布链构建通过。新增真实候选 finalizer 失败验证：故障发生在测量/命中构建之后仍不发布结构或附件；错误诊断保留旧 hit，重试后结构与附件共享一次 revision，单独 overlay transform 改变会重绘但不测量，相同附件不发布。
- 发布前故障/完整附件/统一 revision 测试已通过。删除 VNode 的 class/style 规范化与 renderer 的 DOM value 强制写入、class/style 专用 patch；native 输入统一比较和绑定。删除 QuickJS 任意 dirty flag invalidate 入口。原生事件包装携带实际组件 owner 进入 Vue 错误处理；scheduler 递归检查改为生产也执行，避免单个 JS flush 绕过 QuickJS 微任务预算。
- 新增动态 slot/v-for/nested destructuring、动态组件替换、值 getter 和原生事件错误归属/依赖恢复的行为验证。未处理错误默认在生产也上抛到宿主，避免打印日志后继续提交不完整帧；显式组件错误边界仍可处理并继续。41 项既有 runtime 测试和 TypeScript 检查通过，新场景正在执行。
- 动态 slot 测试首次执行发现测试手工 render context 缺少 $slots，已按正式组件上下文补齐；组件替换及错误边界两项新行为已通过。
- 清除 compiler-arrange 已被正式原生入口替代的全部 DOM transforms、HTML nesting/static stringify、DOM runtime helpers/errors/browser decoder，共 14 个源码文件。真实 Demo bundle 与 Standalone 最新构建成功；GUI 工具遇到系统锁屏，已请求主人手动解锁，自动化验收继续。
- 接通具体 Modifier 实例输入的 QuickJS 正式协议：成功发布后可查询 native 实例 handle，按 handle 注册 binding、写入单个类型化 element；禁止改变实例类型，不使用字段路径。发布同步回收退休实例的 JS binding；单层回调更新不释放其它层，原生最终引用集清理失去所有者的 Modifier 事件。节点/index 参数改为严格 uint32 校验，补入 QuickJS 提前拒绝更新的计数。正在构建验证。
- 具体实例协议已构建通过；新增真实 QuickJS 验证：同节点重复 clickable 单层替换两次、其它层回调保留、keyed 重排后旧 binding 仍写原实例、退休前置拒绝 payload、JS/native 资源回落及拒绝计数。清除单层回调时不登记空回调，未知原生节点和非精确 node id 直接拒绝。
- 输入会话现在在候选帧 finalizer 中同步已提交 modelValue：本地编辑回执保留光标/选择/撤销记录，外部替换清除旧撤销与 IME 临时范围并按 UTF-8 边界夹取选择。指针回调中的旧树读取不用于覆盖尚待提交的编辑。变换输入测试加入聚焦后外部中文 modelValue 替换。
- 审计发现 QuickJS 未跟踪 Promise 未处理拒绝，可能吞掉 Vue scheduler 微任务中的异常；新增 rejection tracker，在同轮微任务稳定后才判未处理错误，同轮 catch 正常解除。scheduler 同时限制整个 flush 工作量，防止不断生成新 job 绕过单 job 递归检查。删除仅抛错的 renderArrangeRoot 历史空壳及导出。
- 真实 SFC/native 集成测试扩展到生产 RuntimePackageBinder + FramePumpDriver：连续四次 HMR context 替换、旧已排队事件拒绝、首帧附件单次发布、JS/native binding/callback 数量回落。生产 HMR 明确采用完整 context 重载，Vite 不叠加组件级 HMR。
- 新增 packages/runtime/README.md，明确现有实现的 setup、结构和值域、watch/生命周期/nextTick、VBlank 原生完成、typed 实例绑定以及失败/reload 语义；未修改施工总纲。
- 新增生产 FramePumpDriver 的资源故障验证：缺失图片在候选准备时报错，旧 scene/hit 保留、错误帧仅一次发布、context 停止；重复 passive paint 不运行测量或发布，reset 后恢复。QuickJS 实例写入及回调回收的新增测试已通过。
- 生产资源故障/被动 paint/reset 和真实 SFC 四次 HMR 验证已通过。Promise 测试首次失败来自脚本没有挂载原生节点（executeModule 的正式装载要求），已补齐挂载；测试宿主 updateBinding 的类型改为跟随扩展后的协议。44 项 runtime 测试已全部通过，正在复验 TypeScript 和 Promise 用例。
- 复验 Promise 同轮 catch/未处理拒绝通过。Modifier、binding、节点代际和事件资源改用统一身份分配器，避免不同 handle 域的数值碰撞；已发布实例查询按真实洋葱链次序返回，重排不改变 handle。
- 移除 QuickJS 旧 nativeError 粘滞旁路：已被 JS/组件错误边界捕获的异常不会在后续调用中再次强制报错。错误统一通过真实 JS 异常与 Promise 拒绝追踪传播；未捕获错误仍触发生产停止/错误发布。显式绑定测试补入捕获 schema 错误后继续成功提交的场景。
- 删除 JS/native 两端无语义的 beginTransaction/endTransaction 空壳，统一提交边界仍由本帧有序 operation 流封闭；Framework 不再可选调用 create/insert/remove/delete/unmount，缺少正式结构能力直接报错。移除 Framework 类型里未使用的 setX 旧入口；内部协议号升为 2，避免旧 bundle 与新宿主被版本检查误认为一致，包发布版本未变。测试改为验证真实 Root/绑定调用。
- 协议版本核验暴露 version-contract 脚本仍指向已删除的 cli/src/constants.ts；当前 CLI 实际从 package.json 读取版本/compatibility。同步和核验脚本改为检查这个真实来源，删除旧 constants 生成，不复活旧 CLI 文件。
- repaint 决策也只读取 PublishedFrame，删除依据外部诊断/session 状态和未变换局部矩形猜测重绘的旧分支；当前整场景 DrawOps 与变换 overlay 对应整视口重绘，后续局部缓存仍属第 4 步。移除无消费者的附件 repaint bounds 字段；开发重载轮询命名去掉已不存在的 Timer。
- 版本契约核验、TypeScript、44 项 runtime 与 Demo bundle 通过。补入递归 watch 的实际失稳用例，并以 NODE_ENV=production 跑值域测试，确认生产 scheduler 不会阻塞原生帧；随后执行最终原生/Standalone 重链接。
- 统一 FrameFinalizer 已成为生产发布边界：候选 scene、DrawOps、HitTest、焦点输入 overlay、诊断 DrawOps 和资源准备一起成功才交换 PublishedFrame；失败保留旧 scene/hit/DrawOps，错误 context 停止，reset/reload 恢复。PassivePaintRenderer 和 repaint adapter 只读取 PublishedFrame，不读取可变 scene/session/diagnostics 作旁路决定。
- 输入几何完成实际变换验证：祖先 transform/scale/clip、padding、content 坐标逆变换、caret/IME 根坐标、disabled 祖先、generation retirement、外部 modelValue 同步、同 revision wheel 累加均有持久测试。
- QuickJS 绑定边界完成：Host/Modifier chain/具体 Modifier instance/Event typed binding，发布后实例 handle 查询与单实例更新，keyed 重排保持身份，同节点重复输入层独立回调；旧 handle 在 payload 解码前拒绝，JS/native callback/binding 退休回落，严格 uint32/bigint/finite reader，Promise 未处理拒绝进入宿主错误边界。
- Arrange Vue 完成结构/value 执行域、组件 props/attrs、普通表达式、computed/helper、动态 spread、文本、事件、scoped slots/default/alias/nested destructuring、dynamic component、branch/keyed 生命周期和 HMR context replacement；删除 DOM class/style normalization、DOM compiler transforms、浏览器宿主和 renderArrangeRoot 旁路。补充 README 记录正式语义，未机械复制 remember。
- 删除 begin/end transaction 空壳和任意 invalidate dirty flag 旁路；结构 operation 流、typed binding 和单一 VBlank FramePumpDriver 是生产入口。协议版本从 1 升为 2，防止旧 bundle 与新宿主误配。
- 最终验证：`pnpm test:runtime` 45/45；`tsc -p tsconfig.json --noEmit` 通过；`verify-version.ts` 通过；根 CTest `arrange_` 8/8 通过；Demo UI bundle 构建通过；Standalone 最新源码重链接通过。系统锁屏期间无法完成最后一项真实窗口人工点击/键盘/滚轮验收，未伪称通过。
- 实机已解锁，最新 Standalone 启动成功。工具点击/键盘暂未产生预期变化，已请主人直接操作交叉核验；不将截图中的既有计数冒充自动化点击成功。原生 CTest 复核 8/8 通过。
- 最终审计发现脚本 reload 的真实断链：QuickJS 两套 reload 账本只有测试读取，诊断 reload 只做 full invalidation。已删除重复标记，将两个 JS 入口合并为同一宿主 action；ArrangeRuntime 增加可消费 reload 请求，未消费诊断/action 也保持 VBlank 唤醒。正在接入 EditorSceneHost 的帧边界消费者并补行为验证。
- EditorSceneHost 已接通脚本 reload 消费；F5、公开 reload 和错误画面 retry 也改为只排队，由下一次 VBlank 加载新 context 后再执行本帧。HMR 复用同一排队/消费位置；删除输入回调中直接加载包的路径。旧 QuickJS smoke 改为检查真正的单一 action 队列，不再断言无人消费的 reload 标记。
- 增加真实 EditorSceneHost→PackageRuntimeLoader→QuickJS→被动绘制的 reload 行为验证：通过实际绘制像素确认手动/HMR/脚本 reload 在帧边界切换包，动画回调中发起的 reload 在无其它工作时保持时钟并最终停止。另补齐无 pipeline 帧的 finalizer 异常捕获，统一进入保留旧帧/停止 context/诊断恢复边界；当前等待编译和运行验证。
- 真实 reload 像素验收、QuickJS smoke 与 SFC/native 集成三项已通过。脚本事件在 module 执行结束之后提出请求也会真正重载；两种 JS reload API、手动和 HMR 都经生产包加载器切换内容并停止空转。补入无 pending scene work 但包状态无有效根时的 finalizer 故障，验证异常不会逃出宿主，且不伪跑测量。
- Standalone 与所有原生测试目标已重新链接。Demo 清除未被任何界面读取的 rainbow Ref/RAF 无限循环，保留真实计数器跨组件颜色/offset 动画，避免空闲 UI 在后台求值无用动画；滚动文案不再声称同步“尚未实现”。开发端点验证脚本替换 Node 26 已删除的 transform-types 参数为项目统一 tsx loader。启动正式 Vite dev server，继续实际窗口 HMR 验证。
- 实机工具交互已取得直接证据：显式左键连续点击使计数 0→5，跨组件颜色变蓝且 offset 向右；输入 Arrange 123 后大标题同步更新，回车显示 Submitted: Arrange 123；滚轮实际显示隐藏行。Vite 保存后窗口从 Debug dist fallback 切换为 Debug live，计数/输入/滚动恢复初态且新文案显示，确认真实 websocket→重载→窗口更新。中文粘贴工具超时、Unicode typeText 未显示变化，未将这两次工具尝试记为中文输入成功。
- 本轮所有原生改动完成后 CTest 8/8 通过（含 idle finalizer 故障与真实 reload 像素验证）；正式 Demo 新 bundle 构建通过，dev endpoint 200 且无残留宏/process.env。运行时 README 补充重载排队/帧边界/时钟存活语义，最新 bundle 已拷贝到 Standalone；退出旧进程后准备复测新原生构建。
- 最新 Standalone 进程已从重新链接的程序启动。正在临时注入 SFC 语法错误做实机 HMR 失败/恢复验收，已保存原 App.vue 以便立即恢复；此临时错误不属于交付源码。
- 实机已出现 Debug error 与 ScriptRuntime Error 画面，显示 HTTP 500、App.vue 源码路径和具体错误行；临时语法错误已撤回，恢复注入前的完整源码，等待真实 HMR 恢复画面。
- 最新原生构建的实机 HMR 恢复已确认：撤回错误后自动回到 Debug live 正常页面，计数/输入/滚动重新初始化，没有停在错误页。临时注入源码和临时备份均已移除。

## 1-3趴验收结论

第 1–3 步已完成；未修改总纲，没有将第 4–7 步提前宣称完成。

| 步骤 | 正式实现与退出证据 |
| --- | --- |
| 1：原生 UI 执行模型 | descriptor/实例身份/协调退休、逐层 measure/place、绘制与命中共享几何、具体实例回调均接入正式 QuickJS 与 Demo。`arrange_modifier_runtime`、`arrange_quickjs_modifier`、`arrange_frame_submission` 覆盖重复/顺序/keyed 复用与退休、内外层尺寸、变换裁剪命中、输入及滚动。 |
| 2：提交与帧执行 | typed binding 和结构 operation 统一提交；VBlank 单一视觉入口、独立原生阶段、候选帧原子发布、被动 paint、故障停止及重载恢复。`arrange_slot_runtime`、`arrange_frame_submission`、`arrange_vue_runtime` 验证阶段计数、过期更新拒绝、资源回落、失败不发布和实际包重载。最终审计发现的 reload 断链和 idle finalizer 异常遗漏已修复并补持久验证。 |
| 3：Arrange Vue | setup 一次、动态结构/value 依赖、props/attrs/scoped slots、computed/helper、分支/keyed/dynamic component、错误边界及作用域释放接入正式 SFC/Vite/Framework。runtime 行为测试与真实 SFC→QuickJS→native 测试证明颜色跳过 measure/place、offset 复用测量、文本触发测量、多层传值不重跑结构、同 Ref 结构和值双消费者、HMR context 回收。 |

最终验证结果：

- `pnpm test:runtime`：45/45，通过；错误边界测试主动产生的 Vue warning 为预期输出。
- `tsc -p tsconfig.json --noEmit`：通过。
- 本轮原生全量重链接后的 CTest：8/8，通过；Demo 移除无用 RAF 后，实际 Demo QuickJS/JUCE 两项再验 2/2 通过。
- Demo UI 构建、正式 dev endpoint、最新 Standalone 重链接：通过。
- 实机：计数连续点击、跨组件颜色/offset、Input 模型同步/Enter 提交、滚轮显示隐藏行、正常 HMR、最新程序上的 SFC 错误页→源码恢复→HMR 正常画面均有直接可见结果。
- 主人已经实测IME，依旧稳健带派（喜）。

保留运行中的 Demo 与本地 Vite server 供主人继续查看；所有临时故障注入已撤回。后续第 4–7 步的局部缓存/性能、完整动画 API、高阶 Vue 能力系统清理和整应用验收仍按原总纲执行。

## 标题彩虹动画实机观察

- 按主人要求，将 Preset A 标题的 textStyle.color 绑定到 animateColorAsState，以原生 requestAnimationFrame/VBlank 每 700ms 切换彩虹目标，约 4.2 秒循环；卸载时取消循环并停止动画。正在构建并启动正式 Demo 供主人观察流畅度。
- 彩虹标题 Demo 构建成功，dist 已更新到 Standalone，Vite/HMR 已加载。实机连续观察确认 Preset A 从蓝色变为橙黄色，循环动画正在运行，Demo 与 dev server 保留供主人直接观察。

## 4–5 趴实施（2026-09-17）

- 已开始按既定总纲施工，不修改范围。审计确认当前阶段跳过仅到整帧层面，测量/放置和绘制构建缺少子树缓存；旧动画仍有 AsState 命名、timer fallback 和独立 child 时间轴。接下来先替换失效传播和缓存，再替换动画与高层组件，并通过真实 QuickJS/native/Demo 验证。
- 第 4 步首批实现已接入生产：节点独立 measure/place 缓存、约束检查与祖先失效；不可变 paint/hit 子树片段在候选帧中共享；Modifier 单层绘制片段按输入/几何/alpha 复用；加入节点、层、缓存命中、完整回退原因与实际阶段耗时计数。此前原生 8/8 回归已通过，正在追加专门的局部执行与缓存一致性验证。
- 删除旧 AsState/timer 动画实现，迁移 Demo，加入全部约定的 animatedXAsRef、解析 spring（保留重定向速度）、tween/snap/easing、每时钟单一采样器与批量反应式通知、Transition 共同时间戳及 owner scope 退休。新增数值组、帧率独立、同步观察一致性与生命周期测试通过。
- AnimatedVisibility/Crossfade 已加入实际结构保留、退出禁用、反向恢复与最终退休；32 项相关 JS 测试通过。animateContentSize 已加入原生类型、JSValue reader、分层测量、绘制/命中裁剪和 VBlank 驱动，正在验证真实原生行为，不以类型接口存在视为完成。
- 模板仅对确认来自 Framework 的 m 固定原厂链及受限参数语法拆分 computed；helper 调用、动态链保持原求值。Framework 按已发布实例 kind/key/handle 提交变化层，结构不匹配退回正式整链协调；相等 typed 输入在提交前消除。新增参数求值次数及 Modifier 分配计数，专项编译行为测试通过。
- 原生专项验证通过：50 个兄弟节点，颜色只构建 2 个节点/1 个 Modifier 绘制层，offset 只放置 2 个节点，尺寸改变只测量 2 个节点、49 个兄弟命中缓存；逐点比较缓存/完整命中结果，逐 DrawOp 比较缓存/完整绘制结果，祖先 clip/旋转/alpha 行为一致。
- animateContentSize 原生行为通过：首次直接自然尺寸、20→100 中点 60、兄弟跟随放置、裁剪命中、稳定子节点不重测、失败候选不污染已发布尺寸、重定向接续、完成/删除释放 VBlank 需求。
- Demo 新增完整 AnimationGallery，自动化 SFC fixture 直接复用同一组件。真实 QuickJS/native 验证已通过：颜色动画不重跑结构/测量/放置，多值 Transition 经 computed/slot，位置/尺寸/内容动画同时推进，退出立即去掉输入命中和焦点，反向恢复、动画期间重排/删除、连续 4 次运行中 HMR 替换与资源回落。
- JS 52/52 已通过，TypeScript 通过。JS 值域耗时使用独立 native steady-clock 读数，避免 VBlank 固定时间戳让耗时永远为零。保留完整候选状态复制及发布显示列表展平的计数，明确当前局部优化是执行/片段缓存而非谎称局部栅格重绘。正在进行最终构建、实机视觉检查和剩余边界审计。
- 主人已实际操作最新 Standalone Gallery，反馈色块移动/变色基本平滑、visible 元素正常显隐、基本不卡。自动化工具的未生效合成点击未计入成功证据；人工结论以主人反馈为准。
- 补齐权重约束传播、resize 不误标全部后代、padding 后基线及 Row Baseline 对齐；持久测试验证基线变化重放置兄弟但复用兄弟测量。动画完成通知改为全部 channel 采样后调用，取消的完成回调不执行。
- 动画数值组追踪实际反应式字段，初值复制为独立快照避免与可变 target 共用对象造成帧前跳值；Size/Rect 尺寸限制非负并保留重定向的已呈现值。Transition child 映射另有实际依赖追踪，外部 Ref 改变可重设该 child 目标，显式 stop 同时退订。
- 将“完整显示列表构建”和“真实被动 raster paint”分开计数：后者只在 PassivePaintRenderer::paint 执行处增长，并记录原因；native 动画采样也移到实际测量层采样入口计数。内部协议升级到 3，包发布版本未改。
- 按原总纲完成第 4–5 步，未改写施工计划。正式生产链已使用独立 measure/place 缓存、Modifier 层与子树 paint/hit 片段缓存、相等 typed 输入消除及安全的内建 Modifier 参数拆分；旧 AsState 动画和 timer fallback 已由 Ref 动画及统一 VBlank 采样替换。
- 全套 animatedXAsRef、tween/spring/snap/easing、多值 Transition、原生 animateContentSize、AnimatedVisibility 和 Crossfade 均有正式 Demo 消费。退出保留结构、禁用交互及清理焦点、反向接续、最终退休、运行中删除与 HMR 回收均有行为验证。
- 最终验证：`pnpm test:runtime` 55/55 通过，`pnpm typecheck` 通过，版本契约核验通过（协议 3）；最终 `ctest --test-dir build/m2-native-debug -R arrange_ --output-on-failure` 8/8 通过。Demo UI 与 Standalone 构建通过，最新 UI 已复制入应用资源，退出旧进程后重新启动，Gallery 正常显示。
- 局部工作量证据：50 个兄弟节点中，颜色更新构建 2 个节点/1 个 Modifier 绘制层，offset 更新放置 2 个节点，尺寸更新测量 2 个节点并命中 49 个兄弟的测量缓存。缓存与完整绘制/命中结果一致；真实 SFC→QuickJS→native 页面证明纯颜色动画不增加结构、测量或放置计数。
- 实机视觉验收采用主人实际操作反馈：色块移动和变色基本平滑，visible 元素正常显隐，基本不卡。最后重启后的截图另确认最新程序正常加载；不将工具未生效的合成点击记为成功。
- 当前明确成本：候选场景仍复制节点状态，发布仍展平显示/命中片段，JUCE 被动 paint 仍按 graphics clip 重放整视口显示列表；相应执行次数及完整回退原因已记录。本轮完成的是局部阶段执行与片段缓存，不宣称实现局部栅格重绘。第 6–7 步仍按总纲另行施工。

## 6–7 趴实施（2026-09-18）

- 简单 review 后，主人授权先清理两项：无消费者的 Options API 公开入口、枚举值任意字符串的静默回退。实机验收暂缓，后续 Windows/macOS 对照；Debug dist 与 Debug live 都出现转圈光标、动画继续但滚动点击无响应，Release 完整页不卡。本轮不调查该交互问题
- 正在删除 Options API 类型/执行残留与无效 mixin、运行时模板配置，编译器和运行时共用正式组件配置集合；TS 已收紧图片缩放、各方向对齐、文本对齐和 Arrangement 类型，原生增加枚举诊断。核对消费端发现 Arrangement 非默认排列和部分二维对齐未正确执行，正补齐行为回归；未实现的字体族/字重和 Input 文本对齐入口一并移除

- 主人已授权完整施工第 6–7 步。新增及修改代码采用中文自然语言文案，注释行末不带句号，单行表达式不因宽度折行，逻辑分组留空行；不为保留旧测试或中途可编译添加兼容实现。
- 工作区开始时干净。已复核总纲、前五步最终记录及架构母文档。审计发现 compiler/runtime 对 class/style 的专门拒绝会误伤用户组件同名 prop；SFC 包仍有 CSS 编译、浏览器资源标签变换；公共导出残留 hydration/SSR 能力；状态 helper 含无原生消费者的假实现。先统一宿主 schema、公开 API 和保留能力生命周期，再推进完整 Demo 与 Debug/Release 真实链路验收。
- 已建立 compiler/Framework 共用的正向 host schema；普通组件的自定义 style/class 可正常传值，删除专项 Web 拒绝测试。SFC 删除 CSS/style 编译、CSS 变量与浏览器资源标签转换；runtime 删除 hydration、旧兼容分支及相关导出，KeepAlive/Suspense 改用明确的脱离场景存储容器。移除未接原生消费者的 Canvas/Lazy/Interop 假公开入口，滚动状态改名 createScrollState，去掉假 animateScrollTo。
- fork 只保留 Arrange 生产需要的源码；删除不进入本项目测试入口的上游 Web/SSR 等测试副本，保留 LICENSE，维护来源说明待统一。实际行为测试仍由本项目 tests/runtime 与 cpp_tests 承担。
- 当前 TypeScript 检查通过，runtime 57/57 通过，新增用户组件同名 prop、源码位置诊断、KeepAlive 反复停用/激活资源退休与一次 setup、Suspense 异步分支发布验证。尚未宣称第 6 步完成：公共类型、剩余清理、真实原生集成与文档同步继续施工。Windows 独立 Debug clean configure/build 已启动。
- 真实 Showcase 与集成 fixture 共用 80 项列表、筛选/重排/删除、KeepAlive 笔记和 Suspense 详情，加入 240 帧混合交互与 C++/QuickJS 分配计量。独立 Debug 原生 CTest 8/8 已通过；首次失败暴露的 MSVC 解释器栈耗尽、旧 Demo 断言和 Windows 图片绘制提交时机均已修正。QuickJS 设置有界 JS 栈预算且 Debug 解释器单独优化，Arrange 自身继续保留 Debug 检查。
- 异步组件/Suspense 延迟改由宿主帧时钟推进，完成/卸载取消需求；真实 QuickJS 验证加载提示、超时、迟到完成与运行中 HMR 回收。继续删除浏览器 DevTools、CSS scope 与 SSR 编译残余；最终 Debug/Release clean build、性能报告及正式 API 文档正在收尾，尚未记录本轮人工体验通过。
- 生命周期与开发体验收尾新增三项测试，runtime 达到 60/60；手写 render 的公开 props 类型、源码行列诊断、顶层 await 退休及 Suspense 等待分支替换均通过。资源来源沿 typed source 输入传到 DrawOp，真实缺失图片错误现在能报告 FrameApp.vue 的原始行列。TS-first 校验同步到 CLI 已有 dist/Entry.js 契约。Standalone Debug/Release 与 VST3 均已构建，正在做最终原生重链接和采样。
- 最终审计收紧 Modifier 公开参数与原生字段校验，移除无正确绘制实现的渐变/阴影入口；继续删除 CSS Module、Custom Element、SSR hook、兼容字段和无用编译标志。实例 watch 正式绑定组件作用域，新增观察/退休验证。母文档同步当前 API 与后续设计边界，未改写任务总纲。
- 最终自动化：runtime 61/61、TypeScript、TS-first 包契约和版本核验均通过；独立 Debug/Release 原生 CTest 各 8/8；两种配置 Standalone/VST3 构建成功，最新 UI 已同步，dev endpoint 200。已启动最新 Debug Demo 与开发服务供主人操作。
- 240 帧真实页面混合负载最终采样：Debug 平均 4.47083 ms、P95 19.8911 ms；Release 平均 0.650873 ms、P95 5.4135 ms。分配量、执行次数、计量边界、复现命令和产物记录于 [Part 6–7 实现与验收记录](6、7趴的实现与验收记录.md)。Debug 并非全帧满足 60 Hz，不将 CPU 流水线耗时等同于真实屏幕呈现耗时。
- 第 6 步实现与自动化验证完成；第 7 步实现、构建、自动化和性能收尾完成，**本轮人工体验待主人验收**。按测试策略保留该项待验，不把历史反馈或自动化模拟视为本轮人工通过，也不提前宣称 M2.3 全部退出。
- 主人人工反馈完整 Demo 输入/点击似乎无响应，重启后继续排查。按主人要求，临时注释 App 中的资源/装饰、滚动示例、ShowcaseGallery、AnimationGallery，并暂停持续彩虹动画，仅保留基础标题、计数按钮、输入框和状态反馈。该页面用于判断负载因素，尚未确认根因，不能将减量视为问题已修复。
- 主人确认减量后的基础页面不卡。随后按要求恢复完整组件与持续彩虹动画，重建 UI、核验 Release 原生构建并同步随包资源，启动 Release Standalone（useDist）对比；完整 Release 的交互体验仍待反馈，尚不能据此归因于 Debug 性能或组件数量。
- 主人确认完整 Release 页面不卡，任务管理器截图显示 CPU 约 2.6%、内存约 26.1 MB。为继续区分构建开销与 live 路径影响，按要求临时注释 Demo 的 Debug useLive 配置，重建并启动 Debug dist；随包 app.js 与刚才 Release 使用的内容哈希一致，完整组件及持续动画均保留。Debug dist 的人工结果待反馈。
- 主人后续确认 Debug dist 与 Debug live 相同：光标转圈、颜色动画继续、鼠标滚动和点击不响应。按要求暂不调查，后续安排 Windows/macOS 实机验收
- Review 第 1、2 项已完成：删除 Options API 与运行时编译的公开类型、执行残留和缓存；正式组件配置在编译期及运行时明确诊断，生产同样拒绝。收紧所有已公开的对齐、图片缩放、文本对齐和排列枚举，补齐真实排列消费及二维对齐；移除没有消费者的字体族/字重和 Input 对齐配置
- 本次验证：runtime 64/64、TypeScript、TS-first 包与版本契约通过；原生 Debug/Release CTest 各 8/8。两种配置 Standalone/VST3 已重建，完整 Demo UI 同步到四处随包目录并核验哈希。没有启动 GUI，没有改动 Debug useDist 对照配置；原始日志和验收边界已同步至 [实现与验收记录](6、7趴的实现与验收记录.md)
- 主人要求 clean build 后重新打开 live Debug；已恢复 Debug 的 useLive。自动审批拦截清理旧目录的命令，改用此前不存在的 `build/demo-clean-live-debug-20260918` 从零配置和编译，不复用旧构建产物。已重启开发服务，9178 正式 bundle 返回 200；编译与启动结果随后记录
- 全新目录的 Debug Standalone/VST3 构建成功，日志为 `build/m23-clean-live-debug-build.log`。已从该目录启动 Standalone（PID 48540），live 服务 PID 676、端口 9178；完整 Demo 保留，交互结果由主人实际操作反馈
- 主人反馈 clean build 后 Windows Debug live 仍可显示画面，但鼠标转圈、点击和滚动无反馈；截图为 CPU 3.2%、内存 71.4 MB。同时 macOS Release 能显示并流畅交互，表现与 Windows Release 一致；macOS Debug live/dist 按同款 O2 配置后均可显示和鼠标交互，但明显卡顿。本机读取为 32 个逻辑处理器，截图总 CPU 约等于一个逻辑处理器的满载量，尚未证明耗时集中在哪个线程。本轮仅记录跨平台证据，未修改代码或认定根因
- 主人授权抓取线程证据。原 Demo 已退出，重新启动同一 clean build 的 Debug live（PID 38668），按窗口句柄确认 UI 线程为 9520。使用本地外部采样工具和当前 PDB 抓取 500 份调用栈，未修改生产源码、二进制或配置
- 实测 UI 线程接近满载：不挂起采样的 5.026 秒窗口消耗 4.969 秒 CPU，约为单个逻辑处理器的 98.9%。500 份栈中 457 份经过文本绘制，412 份经过 TextLayout::createLayout，469 份经过 Direct2D handleDirect2DPaint；QuickJS JS_Call 仅 2 份，SceneFramePipeline 为 21 份。各项为包含关系，不能相加
- 代码对应：repaintDirty 每次请求整个 editor 重绘，PassivePaintRenderer 重放发布列表；JuceDrawOpsPainter 在每次文字绘制重新创建 AttributedString/TextLayout，且未在排版前跳过裁剪外文字。Windows 的采样绘制主要由 swapchainReadyMessageID（WM_USER + 124）触发，存在重绘占用消息线程、挤压输入处理的证据；尚未直接测量鼠标消息积压时长，不能声称已证实输入被过滤，也不能将 macOS 的热点直接等同于此采样
- 原始栈、摘要与无挂起 CPU 测量分别为 `build/m23-live-debug-stacks.txt`、`build/m23-live-debug-stack-summary.json`、`build/m23-live-debug-cpu.json`；采样总挂起约 1.260 秒、单次最长 4.28 ms，统计比例仅用于定位主要热点。本轮完成诊断，没有实施性能修复，旧 CPU 流水线性能数据不包含这里识别出的 JUCE 实际文本排版/绘制成本
- Windows 性能调查按主人指示暂停扩展，已整理 [阶段报告](Windows性能调查阶段报告.md)：真实交互、线程 CPU、500 份栈对照及一次 Debug 20 秒分项计时。实际 paint 平均约 89.5 ms，大部分耗时进入文本排版，裁剪外文字仍参与；纯颜色动画未执行 measure/place/hit。Release 分项、输入队列及重复来源字符串 A/B 尚未完成，不将阶段结果冒充完整验收；正式运行时未改动

## 8–9 趴实施（2026-09-18）

- 主人已授权正式完成第 8–9 Part，范围及退出条件保持总纲要求。开始核对正式源码；确认测量与 JUCE paint 分别排版、Input 存在前缀宽度几何旁路、发布仍展平共享绘制片段。先统一不可变文本结果及后端资源，再贯通片段发布与两级可见性剔除；按中文文案、单行表达式不折行和逻辑分组要求施工
- 开工时两部分尚未实现；此前第 7 Part 的平台/人工留项与性能调查证据仍按原记录保留，不把诊断副本当作生产能力
- 已接入不可变文本排版结果、后端字形资源、节点持有与有界 LRU；JUCE paint 删除即时排版，输入的前缀宽度旁路迁到共享几何。正式发布改为局部坐标的共享片段，位置保留在引用边，操作和整片段可见性剔除均已接入。既有原生 8 项回归通过；新增专项正在核验换行、资源寿命、真实重放和开关剔除图像一致性，尚未宣称退出
- 80 项原生列表专项已确认滚动复用稳定文本与子片段、内容命令不重建、测量不增加，重复 paint 不新增排版；候选准备失败保留旧帧、屏外更新后滚回正确。已补齐后端末尾换行产生空行的语义差异；真实 SFC 集成已切到 JUCE 文本服务，继续追加真实绘制计量
- 最终清理已删除旧测绘双头、单行输入前缀估算、发布展平与无执行来源的旧计数；诊断文字延续上一帧资源引用。片段只在内容/引用实际变化后分配新对象，滚动不先重建稳定命令再假报复用
- Windows Debug/Release 原生 9/9、runtime 64/64、TypeScript 已通过；完整 Demo UI、两种配置 Standalone/VST3 均构建成功。最终审查补充图片 Crop 可能超出目标矩形的保守处理，并通过同 renderer 开关剔除图像对照
- 第 8–9 Part 的正式实现与 Windows 自动验收完成。真实 Showcase 240 帧的实际 paint、glyph 提交、必要排版和片段跳过计数已记录；稳定文字、纯颜色动画和往返滚动无新排版。完整证据、冷内容排版成本及 macOS/人工留项见 [Part 8–9 实现与验收记录](8、9趴的实现与验收记录.md)，稳定契约已同步架构母文档，不将自动化或离屏图片当作本轮人工体验通过
- 最终源码的 Debug 全量 CTest 再验 9/9 通过，Release 全量同为 9/9；Debug/Release Standalone/VST3 四处随包 UI 与本次构建产物 SHA-256 一致，差异格式检查通过
- 按主人要求启动本轮最新 Debug Standalone（`build/demo-ninja-debug`，PID 55760），Debug 配置启用 useLive；复用 9178 开发服务，正式 bundle 校验返回 200，供主人实际交互验收
- 主人实际体验本轮 Windows Debug live 后明确给予性能正面反馈；该反馈确认本轮性能体验，不扩张为 macOS 或全部输入/IME 场景通过
- 按主人要求完成自审，独立探针复现三项 P2：自动换行后的全选区域错误（原核心算法遗留）、显式换行被计入行宽、JUCE ligature placeholder 在几何提取前被跳过导致组合字符定位不一致。复现源码与输出为 `build/m23-review-probe.cpp`、`build/m23-review-probe.log`；本轮 review 未修改生产实现，三项待修，详情见第 8–9 Part 验收记录
- 主人授权修复三项自审问题，开始改为按当前行计算选区、自动换行边界归下一行；JUCE 绘制仍跳过占位字形，输入几何保留其前进量，行宽排除换行并保留空格。正在增加真实后端的几何与图像回归验证
- 三项修复及新增行为验证已通过 Debug/Release 原生全套各 9/9；组合字符点击逐点对照 JUCE 后端，文字对齐及组合字形/家庭 emoji 绘制逐像素对照通过。Debug Standalone/VST3 已重建，Release Demo 仍在链接；架构母文档、总纲和验收记录同步修复状态
- 修复后的 Debug/Release Standalone/VST3 均构建完成，日志为 `build/m23-review-fix-demo-debug.log`、`build/m23-review-fix-demo-release.log`。三项自审问题已关闭，本轮未启动 GUI，修复后的实机交互及 macOS 仍按既有验收边界保留
- 按主人要求重新构建完整 Demo UI 并核验最新 Release 原生构建，随包 `app.js` 与本次 UI 产物 SHA-256 一致；已启动 Release Standalone（PID 50092，useDist），窗口「Arrange 原生画廊」正常建立并响应，供主人实际验收。构建日志为 `build/m23-release-launch-ui.log`、`build/m23-release-launch-build.log`
- 按主人要求启动三项修复后的 Debug live（`build/demo-ninja-debug`，PID 15356），复用 9178 开发服务，正式 bundle 校验返回 200；窗口与 live 连接已确认，供主人继续实机验收
- 按主人要求对三项修复做简要复核：检查行内选区、软/硬换行归属、占位字形几何与去重、绘制数组及回归断言，未发现新的可确认问题；核对既有双配置 9/9 日志，未重复运行测试或改动生产代码

### 本会话交付与交接汇总

- 范围与规范：主人从阅读总纲、对齐颗粒度转为授权正式完成第 8–9 Part，明确允许 breaking change；按中文自然语言文案、原本单行表达式不折行、逻辑组间留空行的规范实施，没有通过降低优化要求、缩减完整 Demo 或保留旧旁路来迁就旧测试
- 第 8 Part 交付：统一测量、Text/Input/占位符、输入覆盖层及诊断文字的共享排版资源；落实节点持有、有界缓存、环境失效与资源寿命计数；删除 JUCE paint 即时排版和输入前缀宽度估算。稳定 paint、纯颜色更新和往返滚动均有不新增排版的行为验证
- 第 9 Part 交付：正式发布与 JUCE 消费改为直接复用共享绘制片段，位置放在引用边；删除生产整树展平及旧递归生成入口，保留同一片段模型的诊断导出。完成操作级与整片段剔除，核验变换、裁剪、透明度和状态恢复；对可能超出目标矩形的图片 Crop 保留绘制，并补充真实图片对照
- 首次交付验证：新增 `cpp_tests/arrange_retained_paint.cpp`，既有真实 SFC/QuickJS 场景改用 JUCE 文本服务并实际绘制。Windows Debug/Release 原生各 9/9、runtime 64/64、TypeScript、完整 Demo UI 及两种配置 Standalone/VST3 构建通过，四处随包 UI 哈希一致；完成 80 项列表、失败候选保留旧帧、屏外更新、节点代际和运行中 HMR 回收验证
- 性能证据与实机反馈：对真实 Showcase 240 帧记录实际 paint、glyph 提交、排版和剔除计数，另记录冷内容排版成本，原始采样及计量边界集中在 [实现与验收记录](8、9趴的实现与验收记录.md)。随后按主人要求启动 Debug live，确认正式 bundle 返回 200、窗口为「Arrange 原生画廊 [Debug live]」且已连接 9178；主人反馈「性能非常带派」「夯爆了」，记为本轮 Windows Debug live 的正面性能体验证据
- 自审与修复：主人要求自审后，以独立原生探针确认三项 P2，并在授权后全部修复：自动换行选区改为当前行计算，软换行边界归下一行；行宽排除换行但保留真实空格；占位字形不重复绘制，其字符区间和分摊前进量完整参与输入几何。加入全选/反向/部分选区、软硬换行、空行/尾随空格、对齐图像及组合字符点击与绘制对照
- 修复后最终验证：Windows Debug/Release 原生全套再次各 9/9，通过既有资源复用和裁剪回归；两种配置 Standalone/VST3 均重新构建完成，差异格式检查通过。最终日志为 `build/m23-review-fix-{debug,release}-{build,tests}.log` 与 `build/m23-review-fix-demo-{debug,release}.log`；这一轮为 C++ 几何修复，runtime 64/64 和 TypeScript 的记录属于此前第 8–9 Part 正式交付时的验证
- 文档与后续：总纲、本文、实现与验收记录，以及文本输入/渲染/帧阶段母文档已同步；三项自审问题已关闭。当前待办是修复后完整 Windows 输入、选区、IME、resize、HMR 和错误恢复等人工场景，以及 macOS 构建、自动化与实机体验；不得将启动窗口、离屏像素验证或修复前的性能褒奖扩张为全部场景验收通过，也不重复开展已完成的第 8–9 Part 实施