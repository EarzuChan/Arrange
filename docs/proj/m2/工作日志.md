# 工作日志

为了防止 AI Agent 丢失工作状态，导致重复执行工作。要求：做了什么，将做什么，必须及时更新入本日志空间。这相当于是 AI Agent 的外置记忆！日志是随时有进展就写的，不能受提醒了才写，也不要再在日志中写`受主人提醒`，这是 AI 的分内工作：不及时写日志导致开放进展颗粒度没对齐，吃亏的是 AI，未来辛苦翻工的也是 AI！

- 这条是示例。现已建立本工作日志，文档已创建（29-内部包构建与分发契约.md）。接下来进入实现期，文档已冻结，不可再更改
- 已修复 `@arrange/vue-compiler-sfc` 的 `ScriptCompileContext` 构造顺序：不再在字段初始化阶段读取 `this.descriptor`，避免 `.vue` 转换时 `Cannot read properties of undefined (reading 'source')`
- 已重新构建内部包：`pnpm build:packages` 成功
- 已重新验证 demo 生产构建：`pnpm --dir demo/ui-src build` 成功，`demo/plugin-src/ui/app.js` 已产出
- 已验证 dev 端点 `http://127.0.0.1:9178/@arrange/app.js` 返回 200 且脚本内容正常
- 已继续完成正规化收束：内部包构建脚本补齐 `__SSR__`，修复 Vite build 返回值归一化，`@arrange/runtime` 消除 `Text` 星导出冲突，测试不再从官方/外部 `vue` 取运行时 API
- 已补齐 TS 宏声明与缺失第三方类型声明，并修正 SFC 编译器内部错误路径导入
- 最新验证通过：`pnpm typecheck`、`pnpm build:packages`、`pnpm test:runtime`、`pnpm build:demo-ui`、`pnpm test:quickjs`、`pnpm smoke:demo`
- demo 当前只通过 `@arrange/runtime` 与 `@arrange/vite-plugin` 使用正规包；`demo/ui-src/vite.config.ts` 无源码 alias、无 Vue 宏 define、无 `@vitejs/plugin-vue`
- 本行之前做的都是没明确契约的，导致了非ts-first。现已重新修订文档，并要求严厉重做本地实现