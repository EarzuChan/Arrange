# 工作日志

为了防止 AI Agent 丢失工作状态，导致重复执行工作。要求：做了什么，将做什么，必须及时更新入本日志空间。这相当于是 AI Agent 的外置记忆！日志是随时有进展就写的，不能受提醒了才写，也不要再在日志中写`受主人提醒`，这是 AI 的分内工作：不及时写日志导致开放进展颗粒度没对齐，吃亏的是 AI，未来辛苦翻工的也是 AI！

- 现已建立本工作日志，文档已创建（29-内部包构建与分发契约.md）。接下来进入实现期，文档已冻结，不可再更改
- 进行了第一次`内部包正规化`，结果方向错了。本行之后按ts-first重来
- 本行之前做的都是没明确契约的，导致了非ts-first。现已重新修订文档，并要求严厉重做本地实现。之后不可再改文档，又进入实现期- TS-first 重做
- 真实状态：内部包 package exports 已改为 `.ts` 入口，`dist`/`.d.ts`/CJS 公开入口由 `scripts/build-internal-packages.ts` 检查并清理；demo 改为通过 `@arrange/runtime`、`@arrange/vite-plugin` 和 `arrange-vite` 使用正规包；生产构建、包契约、runtime 测试、QuickJS 测试曾通过
- 当时未完成点：dev server 的 `@arrange/app.js` 端点仍需重新验证并修正；之前排查时误把后台 node 进程按宽泛 PID 清理，造成 Codex 自身进程被杀，这是严重操作事故。之后禁止宽泛 `Stop-Process node/PID列表`，只能杀本轮自己启动且已记录的子进程，或先用端口 owner + 命令行精确匹配确认
- 29-内部包构建与分发契约已建立 TS-first 正规发行边界；demo 已改为通过 `@arrange/runtime`、`@arrange/vite-plugin`、`arrange-vite` 正规消费内部包，`demo/ui-src/vite.config.ts` 保持无 alias、无 define、无源码直连
- 内部包 manifest 已大规模改为 exports 指向 `.ts` 入口，`scripts/build-internal-packages.ts` 已改为契约验证器，Arrange Vue 源码相对导入已大规模补齐 `.ts` 扩展以支撑 TS-first 消费；Vue 宏已集中到 Arrange toolchain/插件侧收束，不交给 demo 配置处理
- 上轮未完成项已收口：`packages/vite-plugin/src/index.ts` 的不正规结构已拆完；`/@arrange/app.js` 端点已重新验证通过。后续只保留回归检查，不再把它写成悬而未决的开发阻塞
- 本轮已正规化 `@arrange/vite-plugin`：删除随机插件名，删除 `config()` 内递归注入 `arrange({entry})`，不再读取/合并用户 config 作为 dev bundle 内部 build 依据；拆出稳定的 public plugin 名 `arrange-vite-plugin` 与内部 dev bundle transform plugin 名 `arrange-vite-plugin:dev-bundle`，dev bundle 由内部最小 Vite build 配置和 Arrange SFC transform 明确生成
- 本轮已修正 `arrange-vite` CLI：信号只转发给本轮子进程，不再 `process.kill(process.pid, signal)` 自杀；dev endpoint 验证脚本只启动并清理本轮自启动的 `packages/vite-plugin/src/cli.ts` 子进程，不宽泛杀 node/PID
- 新增正式验证脚本 `verify:demo-dev` -> `node scripts/verify-demo-dev-endpoint.ts`：验证 `http://127.0.0.1:9178/@arrange/app.js` 返回 200、包含 app bundle、无未解析 Arrange Vue 宏、无 `process.env`。注意：HMR WS 连接按 `docs/arch/06-热更新.md` 属于 ArrangeEditor 通道，不要求 QuickJS app bundle 内保留 `import.meta.hot` 客户端
- 当前验证结果：`pnpm typecheck` 通过；`pnpm test:runtime` 通过 24/24；`pnpm build:packages` 通过并输出 `verified TS-first internal package contract`；`pnpm build:demo-ui` 通过，生成 `build/demo-ui-dist/app.js`；`pnpm test:quickjs` 通过；`pnpm verify:demo-dev` 通过，输出 `dev endpoint ok: 200, bytes=72621`
- 当前包边界核查：`packages/*/dist` 不存在；内部包 manifest 均无 `main`/`module`/`types`，exports 均为 `.ts` 入口；`demo/ui-src/vite.config.ts` 未出现 alias、define、plugin-vue、源码直连或 dist 消费；生产 app bundle 未检出 `__DEV__`、`__FEATURE_*`、`process.env` 等残
- `demo/ui-src` 的 build 输出已改为 `build/demo-ui-dist`，不再把运行时产物落到源码树；`demo/plugin-src` 的 Standalone 与 VST3 产物在构建时自动复制 `ui/`，Windows 放在 exe 同级，bundle 放在 `Contents/Resources/ui/`
- `config.app.useDist()` 现在只负责记录逻辑包路径，运行时由 `AppResolver` 按发布包根解析；默认路径就是 `ui/`，不再按调用处源码文件位置解析
- 本轮已重新验证：`pnpm build:demo` 在 Debug/Release 都通过；`pnpm test:quickjs` 通过；`pnpm test:runtime` 通过；`pnpm verify:demo-dev` 通过。Release 产物已确认包含 `Standalone/ui/app.js` 与 `VST3/Arrange Demo.vst3/Contents/Resources/ui/app.js`
- 本轮 clean rebuild：发现一个旧的 `Arrange Demo.exe` 仍在 `build/demo-ninja-release/.../Release/Standalone` 后台运行，已按完整路径精确终止；随后删除 `build/demo-ninja-debug`、`build/demo-ninja-release`、`build/demo-ui-dist`，重新执行 Debug 与 Release 的 `pnpm build:demo`，均通过。当前 Debug/Release Standalone 与 VST3 的 `ui/app.js` 均已重新生成，且仓库 build 下没有残留运行中的 `Arrange Demo` 进程
