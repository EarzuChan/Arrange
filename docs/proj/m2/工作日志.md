# 工作日志

为了防止 AI Agent 丢失工作状态，导致重复执行工作。要求：做了什么，将做什么，必须及时更新入本日志空间。这相当于是 AI Agent 的外置记忆！日志是随时有进展就写的，不能受提醒了才写，也不要再在日志中写`受主人提醒`，这是 AI 的分内工作：不及时写日志导致开放进展颗粒度没对齐，吃亏的是 AI，未来辛苦翻工的也是 AI！

- 目前将Vue源码引入了本地并进行了简单接入，但还没开始Arrange Vue的改造。怎么说呢：也就是仅仅使用户不再需要外部引入Vue，而是可以使用内置的Vue了，但反应式与内部组合、布局、绘制的链路重构等等事物还没搞
- 顺手改了UI Dist的打包与发现，修订文档并更正了实现
- 维护文档：“发布与分发边界”整理，TS 包保持 TS-first 官方 npm 发布契约，native 包首发 FetchContent，Demo 继续作为主样板。文档已修改完成，接下来进入代码工作期
- 文档已冻结（目前不可再改），目前正式开始《发布正规化》
- 发布正规化开工检查：已读取工作日志并确认文档冻结；当前工作树仅工作日志未提交。下一步先盘点 package/CMake/Demo 现状，再落地统一版本单源与发布边界
- 新增 arrange.version.json 作为版本单源，版本定为 0.0.0-m.2.0、协议版本为 1；新增 sync/verify 脚本并已同步 root/packages/native/runtime 的版本派生文件
- CMake 已开始转向 FetchContent/用户消费边界：新增 ARRANGE_WITH_JUCE，tests 默认仅顶层开启，公开别名目标 Arrange::core/quickjs/juce 已落地；Demo native 构建脚本改为从 demo/plugin-src 自身配置并通过 ARRANGE_DEMO_ARRANGE_SOURCE_DIR 消费 Arrange
- npm 包 manifest 已改为里程碑版本与可发布依赖规格；public 包 @arrange/runtime/@arrange/vite-plugin 标记 publishConfig/access/tag，并声明 bundledDependencies 收束内部 Arrange Vue 包；新增 .npmrc 让本仓库开发期用精确版本也优先链接 workspace。pnpm install 已刷新 lockfile
- 新增 pack:packages / scripts/pack-npm-packages.ts，采用干净 staging 物理拷贝 public 包与 bundled 内部 Arrange Vue 源码，避免 pnpm junction 让 npm pack 产物出现 package/../ 或测试/bench 文件；已成功生成 artifacts/npm/arrange-runtime-0.0.0-m.2.0.tgz 与 arrange-vite-plugin-0.0.0-m.2.0.tgz，并检查 tar 列表无 package/../、__tests__、__benchmarks__、dist
- verify-version、TS typecheck、package boundary verifier 均已通过。package verifier 现检查统一版本、无 workspace/catalog 发布规格、public 包 publishConfig/bundledDeps、TS-first exports/no dist，并使用 build/demo-ui-dist/app.js 检查 Arrange Vue 宏无残留
- Demo UI 构建已改为执行 demo/ui-src 自己的 package script（arrange-vite），不再从根脚本直接调用 workspace 源码 CLI；已验证 node scripts/build-demo-ui.ts 可通过用户样板路径完成构建
- Detached Demo native 配置首次暴露 top-level 只启用 CXX 导致 QuickJS C 目标缺少 CMAKE_C_COMPILE_OBJECT；已将 demo/plugin-src 顶层 project 改为 LANGUAGES C CXX，继续验证
- Detached Demo 全量构建已通过（JUCE_DIR=third_party/JUCE，ARRANGE_QUICKJS_NG_SOURCE_DIR=third_party/quickjs-ng，Debug）：UI 通过 demo package script 构建，native 从 demo/plugin-src 顶层以 ARRANGE_DEMO_ARRANGE_SOURCE_DIR 消费 Arrange，最终生成 Standalone 与 VST3
- npm pack 产物已用临时 npm consumer 真实安装并构建 demo 源码成功。中途发现 Node 24 不支持直接 strip node_modules 内 TS，已将 arrange-vite CLI 改为 tsx 启动/传递 tsx 给 Vite 子进程，并补齐 vite-plugin 编译器实际运行依赖（hash-sum/lru-cache/merge-source-map/minimatch/postcss-* 等）
- 新增 verify:npm-packages / scripts/verify-npm-packages.ts，将 npm pack tarball 安装进 build/npm-package-consumer 并用 demo 源码执行 arrange-vite build，同时检查 dist/app.js 无 Arrange Vue 宏残留；脚本已通过
- 为 TS-first 的 arrange-vite 增加最小 CJS bin bootstrap（非 dist/API），由它从包自身解析 tsx 后启动 src/cli.ts；src/cli.ts 再把绝对 tsx import 传给 Vite 子进程。该修正确保 npm 与 pnpm/isolated node_modules 下都不依赖用户手动安装 tsx。Demo UI 与 npm tarball consumer 已重新验证通过
- pnpm test:runtime 通过（24 tests）；pnpm test:quickjs 在 ARRANGE_QUICKJS_NG_SOURCE_DIR=third_party/quickjs-ng 下通过，覆盖 demo UI 真实 package script 构建、QuickJS app smoke、严格 modifier、diagnostics smoke
- pnpm test:native 在 ARRANGE_QUICKJS_NG_SOURCE_DIR=third_party/quickjs-ng 下通过，确认原生 core/quickjs/juce stub 目标与 arrange_core_smoke 可构建运行
- 发布正规化验证：pnpm test 全量 TS 测试通过（24 tests）
- 新增 verify:native-fetchcontent，生成临时 FetchContent consumer，链接 Arrange::core/quickjs/juce 并校验 RuntimeVersion；首次暴露 public target 未向消费者传播 C++ 标准，已在 arrange_core PUBLIC target_compile_features(cxx_std_20) 修正，验证已通过
- 发布正规化验证：pnpm build:packages 通过
- 当前发布正规化验证矩阵（Windows，本机）：pnpm typecheck、pnpm test、pnpm test:runtime、pnpm build:packages、pack:packages、verify:npm-packages、verify:native-fetchcontent、pnpm test:quickjs、pnpm test:native、detached pnpm build:demo 均已跑通；mac CI 按决策仍留到本轮之后
- 最终重新运行 pnpm build:demo 通过（Debug，JUCE_DIR=third_party/JUCE，ARRANGE_QUICKJS_NG_SOURCE_DIR=third_party/quickjs-ng），再次确认 Demo UI/native 均走离体用户样板路径并生成 Standalone/VST3
- 统一补齐若干新增/改写文本文件末尾换行后，重新运行 pnpm typecheck 与 pnpm build:packages，均通过
- version-contract 现也纳入 demo/ui-src/package.json，确保 Demo 样板项目版本同样受 arrange.version.json 单源约束；verify-version 与 typecheck 已重新通过
- version-contract 纳入 Demo 后，pnpm build:packages 重新通过
- 工程清理：重写 .gitignore，补齐 OS/IDE/package/build/CMake/log/temp 忽略规则，并将历史误追踪的 .idea 本地工程文件移出 Git 索引；本地文件保留。
- 发布演练准备：移除会让 npm 警告的 .npmrc，改由 pnpm-workspace.yaml 承载 linkWorkspacePackages；verify-native-fetchcontent 增加 ARRANGE_FETCHCONTENT_GIT_REPOSITORY / ARRANGE_FETCHCONTENT_GIT_TAG 模式，并将用户样板链接面收敛为 Arrange::juce。
- 发布演练修正：发现 pnpm script 内调用 npm pack 仍会透传 pnpm-only npm_config_* 造成 npm unknown config warning；已在 npm 子进程环境中剔除相关 pnpm 配置，保持 pack/verify 输出干净。
- 发布演练验证：本地 tag v0.0.0-m.2.0 已推送到 EarzuChan/Arrange；GitHub tag 模式 verify:native-fetchcontent 通过；重新 pack npm tarball 后，@arrange/runtime 与 @arrange/vite-plugin 的 npm publish --dry-run 均通过。
