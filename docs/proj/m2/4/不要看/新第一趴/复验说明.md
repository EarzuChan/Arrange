# 新的第一趴证据

记录日期：2026-09-21。仅对应本轮统一运行时重建；旧 evidence/第一趴 不作为新底座证据。范围、计数及未验事项见 [验收记录](../../不要看：第一趴实现与验收.md)。

在仓库根执行：

```powershell
pnpm typecheck
pnpm typecheck:sfa
pnpm test
pnpm build:demo-ui
pnpm verify:version
node --import tsx docs/proj/m2/4/evidence/新第一趴/verify-native.ts
pnpm verify:npm-package
& build/m23-final-debug/cpp_tests/arrange_quickjs_app_smoke.exe build/npm-package-consumer/dist/app.js
& build/m23-final-debug/cpp_tests/arrange_juce_runtime_smoke.exe build/npm-package-consumer/dist/app.js
```

原生复验脚本增量构建既有 build/m23-final-debug 与 build/m23-final-release，并各执行 10 项 CTest；未重新配置或清空构建目录。Demo 使用既有 build/demo-ninja-debug，构建目标为 ArrangeDemoPlugin_Standalone、ArrangeDemoPlugin_VST3，编译环境通过 scripts/common.ts 的 runInVsDev 初始化。

native-debug-build.txt 是完整目标增量构建，native-callback-build.txt 是随后仅调整测试夹具后的定向构建；native-debug-tests.txt 在两者之后取得。Release、Demo 和包原生输出均在最终原生改动之后取得。TS 最后一次是 99/99，后续只整理文档。

发布包日志中的 npm audit 输出是安装器报告，不表示本轮执行依赖升级或安全审计；CLI 只验证安装。源码扫描、文档链接与空白检查另记 audit.txt。
