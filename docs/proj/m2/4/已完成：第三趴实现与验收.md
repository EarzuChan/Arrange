# M2.4 第三趴实现与验收

2026-09-22。范围为任务清单第 8–10 节，已完成实现和自动化验收。第 11 节整期退出审计不因本记录自动完成。没有提交 Git 或发布 npm registry。

## 交付与证据

| 范围 | 结果 | 对应验证 |
| --- | --- | --- |
| 动态定义 | is、props、具名内容遵守同一声明，完整输入交付，错误保留旧帧 | SFA 正反类型用例、动态切换及事务回归 |
| KeepAlive | 有限 LRU、零项/多项内容保留、暂停任务、恢复最新值、逐出与最终清理 | 原生画廊、逻辑编辑值、暂停观察和动画恢复 |
| 删除项与上下文 | ArrangeLocal、rAF/cancelRAF、公开动画时钟及旧消费者退出，App 默认 Density 沿 provide/inject 覆盖 | 分层入口、构建及公开类型检查 |
| 诊断 | 声明驱动的静态和动态边界，无法提取类型明确失败，来源和期望/实际类型保留 | 真实 SFA、错误参数及原生失败回归 |
| 帧授权 | 首帧、结构、值和动画共用生产调度器，nextTick 仅微任务，提交后写入留到下一帧 | 首帧计数、动画、失败通知、原生提交测试 |
| Owner 与异步 | 输入保序，资源完成通过 message thread 唤醒；无 peer 也可完成语义任务 | 三次输入、资源完成、旧代际、邮箱过载原生用例 |
| 执行预算 | 数量、递归、邮箱及耗时预算，中断终止任务、保留旧帧、重新加载恢复 | 长脚本、长微任务、长视觉回调及失败事务用例 |
| 单位 | SFA 按导入身份拆壳为数字表达式；TS 数字契约、跨 SFA/模块、参数/字段/返回值检查 | 别名/遮蔽/跨文件/算术表达式与错误单位用例 |
| Density | DP/SP 分别转换、PX 直通，响应式局部消费、共享声明不修改、失败保留旧帧 | DP×2、SP×3、不同上下文、停用恢复；原生布局、滚动、命中和 IME |
| Text 名称 | Text 及 text Modifier 使用 style；Input/TextFieldOptions 与类型名保留 | Demo、模板、类型、QuickJS 与原生回归 |

## 最终验证

环境为 Windows、MSVC、JUCE 8.0.12、QuickJS-NG 0.14.0。内部脚本/原生协议升级为 5。

- 全量 TypeScript 测试 133/133：[日志](不要看/第三趴/tests.txt)
- 第三趴定向测试 12/12：[日志](不要看/第三趴/focused-tests.txt)
- Debug 和 Release 原生 CTest 各 12/12：[日志](不要看/第三趴/native-debug-release.txt)
- TS、SFA 类型检查，TS-first 包构建、版本一致性及 TS/C++ 格式检查：[日志](不要看/第三趴/engineering-checks.txt)
- Demo UI 和 Debug Standalone/VST3 构建：[UI](不要看/第三趴/demo-ui.txt)、[原生](不要看/第三趴/demo-native.txt)
- 当前源码打包、独立安装、公开子路径、真实 SFA 类型检查与 Vite 构建、JUCE/QuickJS 交互运行：[发布包](不要看/第三趴/package-consumer.txt)

没有执行人工 DAW、真实显示呈现或多平台验收；自动化 IME 几何验证不冒充人工输入法验收。运行时失败保留已发布帧，不承诺撤销用户 JS 副作用。具体长期规则已同步到运行时、基础类型、SFA、调度、动画、Modifier 和文本母文档，不在这里重复定义。
