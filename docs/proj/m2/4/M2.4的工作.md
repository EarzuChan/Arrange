# 工作日志

为了防止 AI Agent 丢失工作状态，导致重复执行工作。要求：做了什么，将做什么，必须及时更新入本日志空间。这相当于是 AI Agent 的外置记忆！日志是随时有进展就写的，不能受提醒了才写，也不要再在日志中写`受主人提醒`，这是 AI 的分内工作：不及时写日志导致开放进展颗粒度没对齐，吃亏的是 AI，未来辛苦翻工的也是 AI！

以下是正式条目：

## 新的第一趴范围确立与待办重排（2026-09-20，当前口径）

- 主人确认将重排重建、RearrangeNode、完整 RearrangeScope、arrangeValue 删除与延迟求值内化一起纳入新的第一趴；本轮授权仅为文档修订，未修改源码、编译或运行源码测试
- 待办第 1–3 节保留为最早第一趴的历史基础，勾选仅表示曾实施，须在新底座回归；第 4–6 节为新的第一趴，重建条目及退出证据全部未勾选，不继承旧 84 项完成结论
- 补齐对象定义、编译/运行期/原生/FFI 与生命周期职责、删除对象及替代机制；明确 setup 一次、结构调用返回 void、持久调用账本、空输出插入位置、局部作用域重启、全 prop getter 和 Layout 唯一管理节点
- 第二趴改为第 7 节：包整理与原生局部工作量优化；第三趴为第 8–10 节：辅助行为、诊断和统一生产帧调度；整期验收改第 11 节，正文交叉引用同步迁移
- 同步运行时母文档、里程碑、第一趴验收记录与最新 ADR 015；下方较早记录的旧分趴属于当时决策历史，当前范围以上述待办为准
- 本轮 6 份文档的 57 个本地链接、代码围栏、章节顺序与新第一趴未完成状态检查通过，git diff --check 通过；没有执行源码构建或测试

## 统一 Arrangable 与 Layout 重排模型文档修订（2026-09-20）

- 本轮授权只修订文档，代码保持拆除后的状态；未编译或运行测试，也未开始重建
- 已将统一定义、Layout 唯一特权、RearrangeNode 身份和具体重排过程集中到运行时母文档；普通 FA 不再拥有专属注册器、Context 能力、状态工厂或原生入口
- 待办第 4 节、第 5.1–5.2 节重写：删除 VNode 及整个消费者协议，Layout 实现唯一创建/复用/更新/退休 RearrangeNode，普通调用和控制流只有必要实例/作用域账本
- 第一趴返工范围现为第 1–4 节及第 5.1–5.2 节；第二趴保留第 5.3–5.4 节的包整理、精细重启与优化以及第 6 节的 prop getter 全内化；第三趴仍负责辅助行为和统一帧调度
- 文本职责统一到 Modifier：修订稿撤销旧 TextMeasurePolicy 条款，Text/Input 由文本元素测量与绘制，内层空 Layout 使用 MinSizeMeasurePolicy；正式 UI 构造能力可供用户 SFA 使用，无 Foundation 隐藏通道
- 同步 Foundation、Modifier、文本、API、模板、原生边界、相关调度与测试措辞、术语索引、里程碑及验收记录；新增 ADR 015 记录本次取舍，历史 ADR 未修改
- 本次新增和重写的任务均未勾选，旧证据只保留为历史记录；文档供主人检查和二次修订，不把设计确立写成代码交付
- 已核对 17 份相关文档的本地链接与代码围栏，检查通过；清除测试母文档中与统一 SFA 路线冲突的 JSX/render 示例。只做文档检查，不执行源码编译或测试

## 第一趴错误实现拆除（2026-09-20，已停止）

- 主人本轮明确要求只删除错误实现，删除后停止，不补替代实现，不编译、不运行测试；第一趴仍未完成
- 已删除 FA capabilities 的类型、注册器、声明、call 第四参数、调用记录、校验及更新分支，删除 runtime/layoutInvocation.ts；未改名保留旁路
- 已删除 FoundationContext.layout、defineFoundationLayout、布局权限注册与凭证；删除 Layout/Text/Input 中依赖这些协议的 implement，保留参数声明与实现缺口，未补空实现或兼容入口
- 已删除通用 createState/state、retainsContent、ContentHost/ContentRetention 及运行时接入，删除 runtime/src/KeepAlive.ts 的错误缓存实现和导出，未恢复 Vue KeepAlive
- 已删除 LayoutNode 的 textPresentation 模式与直属文本缓存字段、宿主文本输入名称及校验、文本模式测量/绘制分支、按节点模式选择文本输入目标的实现；共享排版与 Modifier 基础设施保留，尚未实现文本 Modifier
- 已删除 foundation.test.ts、两项针对该 KeepAlive 实现的测试及原生契约测试中直接设置文本模式的片段；其余消费者、原生声明和夹具仍有缺失实现或失效引用，本轮不修复这些断口，不以编译通过为目标
- 已清除长期文档对能力旁路、createState 和 ContentHost 的背书，撤回待办第 4.1–4.3 节完成标记及里程碑完成声明；此前测试输出仅为已拆除实现的历史记录
- 未调整 TextMeasurePolicy 的正式要求，未开展 arrangeValue 全内化、通用 RearrangeScope、统一帧调度或替代架构重建；这些不属于本轮删除动作

## 第一趴架构返工（2026-09-20，已否定，以下为拆除前记录）

- 主人撤回上一轮交付认可，授权全面重做第一趴偏离设计的部分；此前“第一趴正式完成”结论失效，旧测试结果仅证明当时实现可运行，不证明 FA/Layout 架构合格
- 已重新核对第 1–4 节及第 5、6、9 节边界，确认当前 defineFoundationArrangable 仍为套壳、layoutNode 返回字符串宿主 VNode、Text/Input 经 layoutInputs 隐式通道交付，均须替换
- 本轮实现专门 FA 定义与 implement 执行、正式嵌套调用、声明并校验的内部能力、Layout 专用结构记录及原生应用入口；验证零根/多根、实例复用、受体独立、值更新和退休错误路径，并重做相关验收
- arrangeValue 全内化、五类对象全量迁移、通用 RearrangeScope 和统一帧调度仍属后续趴；第一趴保留现有值表达式协议及内部 VNode 承载，不以全量改名代替实际职责分离
- 主人已删除 Suspense、Teleport、Vue BaseTransition，并将 KeepAlive 移到 runtime/src；本轮已清除它们的专用结构字段、执行分支、编译 helper、导出与旧测试，不恢复文件。异步初始化不再依赖隐式等待分支，顶层 await 在 SFA 编译期诊断，异步业务函数及显式加载状态仍可使用
- 已实现不可变 FA 定义注册、独立 implement 执行、每实例 createState、正式 call/content、Layout 专用调用身份验证和 typed 文本能力；已删除 h('LayoutNode')、layoutNode、layoutInputs，原生应用使用 createLayout 入口。定义注册与执行拆开，避免循环初始化；直接以 FA 为 App 根时保持定义身份
- KeepAlive 已在 runtime/src 重写，底层只提供声明驱动的 ContentHost 停用/恢复/退休操作，不再依赖 KeepAlive 名称、实例 ctx 注入、旧 shape flags 或整套 RendererInternals。最近验证：类型检查通过，全量 TS 测试 90/90；仍须最终原生、发布包、文档与边界审计

- 已经搓定 [M2.4任务待办](01-M2.4任务待办.md)。

## 第一趴实施（2026-09-20）

- 主人授权正式完成第一趴（第 1–4 节），允许 breaking change；开工工作区干净。按照开发约束执行中文自然语言文案、注释无行末句号、4 空格缩进、单行表达式不任意折行和逻辑组间空行
- 已读取文档治理、第一趴完整要求及相关母文档。当前实现仍有字符串 FA、attrs 透传、emits/model、SFC 双脚本、隐式文本及资源输入路径，必须迁移实际消费者，不通过兼容层维持旧语义
- 实施前命名清单：单文件源码 `.vue` → `.sfa`，工具层 Sfc/SFC → Sfa/SFA、isVueModule → isSfaModule、compileArrangeSfc → compileArrangeSfa；公开组件定义与类型归属 Arrangable，代码 FA 使用内部 defineFoundationArrangable；空 Modifier 起点 m → M。五类对象的全量内部重命名和包合并归第二趴，但第一趴新增协议直接采用正式术语，不增加旧名别名
- 接下来先收紧模板/SFA 编译及参数内容边界，再迁移代码 FA、Layout/MeasurePolicy/Painter 和生产消费者，补充真实编译与运行验证，并据实际结果同步母文档与验收证据。尚未完成任何检查项
- 施工中已开始删除模板隐式协议、迁移 `.sfa` 和 `M`，参数更新改为完整校验后再写入；尚未完成生产消费者和原生能力迁移，不能把当前草稿当作第一趴交付。批量遍历曾误入依赖链接，已强制按锁文件重装依赖并修复已识别的无关源码误改；后续批量修改限定 Git 跟踪的文件清单
- 主人裁定 styleless 的基础呈现：Text/Input 未指定文字颜色时使用固定基础黑色；Icon 未指定 tint 时保留 Painter 原色；字号和字体只使用基础排版 fallback，不从主题或上下文取得样式
- 已删除 runtime 的隐式 Text 构造、文本子内容写入和字符串静态节点入口，Fragment 改用结构锚点；SFA 的 Slot 声明已进入定义元数据，内容调用建立独立 effect scope，不生成 Slot 实例。新增定向用例验证重复调用的实例隔离、内容依赖局部更新、未声明内容拒绝和卸载清理，当前 5 项定向测试通过；第一趴整体仍未完成
- 主人裁定 Crossfade 采用显式 is（定义）、props（目标参数）、targetState（切换身份），退出层保留自身定义及参数，不再使用带参 Slot；正在迁移实现和消费者
- 编译器已收敛为单一无属性 TS setup script，删除双脚本及 emits/expose/options/model/slots 宏模块；运行时已删除 attrs/emits、模板 ref 及对应实例字段。参数声明与对象绑定正在统一名称和重复校验，Ref 类型推断已开始追踪导入身份。上述改动尚未完成全量类型检查与消费者迁移，原生 FA/Layout/Painter 仍须实作，清单仍未勾选
- 已删除 v-once/v-memo 的转换、缓存标记与运行时 helper；代码 FA 已改为正式参数/内容声明，公开参数类型从定义推导。开始接通通用 LayoutNode、五种类型化原生 MeasurePolicy 和显式文本能力，清除原生按 FA 名称选择测量的分支
- Painter 正在接入真实资源请求：QuickJS 持有身份、代际、版本和完成回调，JUCE 工作线程执行解码，Owner 接收结果，paint Modifier 持有不可变绘制内容；退出或 reset 清理回调，已发布帧共享保留旧内容。当前仍在接线和迁移消费者，尚无原生构建通过证据
- 后续定向验证：Debug 的 arrange_core、arrange_quickjs、arrange_juce 已构建通过，新增 arrange_m24_contract 原生测试通过；真实 SFA 类型检查及 6 项新契约测试通过。全量运行时测试仍有 34 项失败（共 70 项），主要为旧协议消费者未迁移；全仓类型检查亦未通过，不能据定向结果宣称交付
- KeepAlive 已开始改为缓存无参内容调用作用域，以显式 cacheKey 区分零根、单根或多根内容；正在补充停用、恢复、LRU、词法闭包更新及卸载测试，修复作用域归属和生命周期问题。第一趴清单保持未勾选
- 主人明确要求 Component 从内部符号到文件名全面改为 Arrangable；已迁移运行时、编译器、类型、Demo 与测试引用及 23 个文件路径，不保留旧名别名。此次命名要求纳入第一趴，不再推迟到第二趴
- 已删除内建共享参数表与共享输入类型，逐个 FA 声明自身参数并显式组装输入；Spacer 只接收 modifier，Row/Column 只接收各自布局参数，Text/Input 独立声明文本参数，Image/Icon 显式接收 Painter。Box/Row/Column/Spacer 已明确调用 Layout
- 新契约与值绑定定向测试 42 项全部通过；修复了 Content 未纳入结构缓存导致卸载遗漏、缓存恢复复制丢失 contentScope、KeepAlive LRU 双重退休风险及生命周期顺序问题。FA 转交参数同步保留源码位置；动态参数更新先验证再替换结构记录。全量运行时测试最新为 72/73，剩余一项旧宏断言正在迁移；尚未完成第一趴整体验收
- 已检查公开 XxxProps 的用途：调用方可用 satisfies 验证参数对象，现有类型回归用例实际消费这些类型，予以保留；删除独立 arrangableTypes.ts，各类型从正式声明推导，直接放在对应 Arrangable 定义上方导出，不另建重复参数模型
- 当前继续第一趴收尾：核查已落地的指令运行时删除、首次挂载任务栈和原生 Painter 测试迁移；最近保存的原生 CTest 结果为 6/10，尚需完成剩余失败修复、编译优化、SFA 工具链与长期文档，第一趴仍未完成
- 全仓 TS 检查与 73 项运行时测试已通过；Vite/SFA 工具链补充源码映射（脚本及模板）、类型依赖 watch/cache 失效、paths 别名解析和具名 Slot 静态契约检查，新增原样 Ref 与 TS 泛型/比较/类型断言边界测试，最新工具链定向 21/21 通过。删除 TEXT_CALL/TextCallNode 和 toHandlers 执行/导出残留
- 原生集成剩余失败已修复：测试宿主配置真实 Painter loader，绑定计数按正式 Policy 输入更新，Showcase 断言匹配业务请求身份；Painter 错误经异步完成帧发布并保留 FrameApp.sfa 绑定来源。JUCE 原生图像需结束 Graphics 后读像素，修正测试与 SVG tint 遮罩的上下文生命周期。最新运行 quickjs_app/juce_runtime/quickjs_modifier/vue_runtime 均通过，完整 CTest 结果待本次日志输出确认
- 下一步持续第一趴：挂载任务栈失败清理及生命周期/深层测试、Text/Input 经 Layout 的显式能力入口、静态声明驱动调用与安全描述缓存、Painter 缩放及退休测试、文档和最终 Debug/Release/包验证，清单尚未勾选
- 完整 Debug CTest 已确认 10/10 通过（tmp-refs/m24-ctest.txt）；其后继续修改了挂载/卸载与 Text/Input 路径，最终原生验收须在这些新改动后重跑
- 挂载失败回滚补充宿主节点移除及成功通知延后，初始 ValueBinding 求值失败会立即停止；卸载与作用域遍历改为任务栈，新增 1800 层实例挂载/卸载、子先父后生命周期与后续分支失败清理测试均通过。Text/Input 显式调用 Layout 并交付内部文本能力，不增加公共通用参数表；Layout 仍校验自己的业务参数。编译器缓存无列表/槽位词法捕获的参数与内容描述，最新全量运行时测试通过；声明位置优化及性能对比仍未完成
- 已实现固定调用的参数位置描述：编译器生成稳定名称位置与延迟值数组，调用点首次连接正式声明，后续验证按位置读取；动态对象仍逐项名称校验，默认工厂保持实例时机。40 次更新对照减少 40 次名称检查、固定路径 80 次位置读取，两条路径结构运行增量均为零，定向用例通过。长期母文档 12 已更名为内建Arrangable并重写，27 已按新模板/参数/内容契约重写，21/18/28 及其他 arch 名称链接迁移进行中；尚未完成文档全面审计
- 续接审计：已完成 Painter 的 42 组 raster/SVG 缩放、对齐与 tint 逐像素对照并修正 Crop 裁剪；删除旧资源 helper 与无效预加载遍历。最近完整验证为 TS 79/79、Debug CTest 10/10；新增真实 Ref 测试误用 shallowRef(existingRef)，现改为独立布尔选择器，正在重验。发布包验证发现现行 CLI build 为未实现骨架，将直接验证正式 Vite/SFA 消费链路，不扩建 CLI。第一趴尚未完成
- 参数组更新改为逐表达式 computed 缓存、整组求值校验后交付实例，补充上下限同时更新和无关表达式不重算验证；固定调用 12 次外层重排比动态对象少分配 12 个值描述，求值次数均为 44。新增测试捕获并删除优化分支残留的隐式 Text 节点判断。1200 层实际 LayoutNode 的宿主账本创建和退休已改任务栈并通过；真实 Ref 身份、泛型别名与只读权限验证通过
- 独立发布包 SFA checker 已验证通过，修复了编译宏类型声明没有随包发布的问题；TS-first 发布形态的 Vite 配置须使用正式 configLoader runner，已验证消费者生产构建通过，将纳入完整验证脚本。正在补 Painter SVG 视口、裁剪以及 reset/失败/迟到完成验收，尚未勾选第一趴

- Debug 与 Release 原生 CTest 均已通过 10/10，Demo Debug Standalone/VST3 构建成功；Painter 增加 reset、销毁后迟到完成、异常 future、旧版本及退休身份拒绝验证，raster/SVG 对照扩至 84 组，半像素插值与覆盖率容差限制为每通道一级。SVG 按视口保留透明留白，不再按图形包围盒确定固有尺寸
- 已清理 attrs 类型扩展旁路、onXxx/model 识别残留、过时 compiler 错误码、v-pre tokenizer 标记及隐式 Text 优化分支。初始化、重排与首次值求值失败必定中止挂载，不允许错误处理器吞错后发布成功生命周期；恢复当前实例、跟踪与告警上下文，补充失败后再次挂载验证
- 最新完整 TS 测试为 88/88，参数/动态对象错误已补真实 SFA 行列来源；KeepAliveProps 从正式定义推导并就地导出。清理无消费者的 CSS/旧映射工具依赖，锁文件已更新，接下来对最终源码重跑类型、SFA、生产包和原生集成并归档第一趴证据

- 第一趴正式完成：第 1–4 节 84 项已逐项核对并勾选，长期母文档、里程碑状态及[第一趴实现与验收](第一趴实现与验收.md)已同步。最终 TS 检查、SFA checker、88/88 TS 测试、Debug/Release 各 10/10 CTest、Demo Standalone/VST3 构建、独立发布包类型/构建/QuickJS/JUCE 运行验证全部通过，9 份输出归档在 evidence/第一趴。发布包验证补齐 public 资源，不再漏掉实际 Painter 文件；历史 ADR 未改，改动未提交。后续继续第二趴时以本验收记录和日志为准，不重做已完成迁移
