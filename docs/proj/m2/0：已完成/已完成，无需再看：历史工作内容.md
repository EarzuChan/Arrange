# 简要工作日志

1.  **版本单源化**：确立 `arrange.version.json` 为全局唯一版本事实源（v0.0.0-m.2.0），自动同步至 root、npm packages 及 CMake 配置文件。
2.  **Native 消费边界正规化**：CMake 架构转向 `FetchContent` 消费模式，对外暴露 `Arrange::core/quickjs/juce` 别名目标，并确立了离体用户消费的标准模式。
3.  **npm 发布正规化**：
    *   确立 `@arrange/runtime` 与 `@arrange/vite-plugin` 为公开包，内部 Arrange Vue 源码通过 `bundledDependencies` 物理捆绑发布。
    *   采用 **TS-first** 发布策略（不搞 dist 不搞 `d.ts`），封装引导逻辑，确保用户侧开箱即用。
4.  **Demo 样板离体化**：重构 Demo 构建链路，使其完全脱离 workspace 源码。UI 模拟从 npm 安装包构建，Native 模拟通过 FetchContent 远程引用，验证了发布边界的闭合性。
5.  **自动化验证闭环**：
    *   落地 `verify:npm-packages`：模拟真实 npm 安装过程并校验 UI 构建产物的宏清理。
    *   落地 `verify:native-fetchcontent`：模拟外部项目链接仓库 Git Tag 并完成 Native 构建。
    *   全量通过 typecheck、Runtime/QuickJS/Native 单元测试及 Smoke 测试。
6.  **发布演练成功**：完成 npm 注册表 v0.0.0-m.2.0 版本发布（含 `m` 标签），并已通过离体 consumer 安装验证及 Release 模式下的 Standalone/VST3 生产构建。