# 目标

Arrange 运行时的目标是让 Arrange Vue authoring 资产驱动 JUCE-native UI execution。运行时必须把 SFC、template、`ref` / `computed` / `watch`、组件生命周期、HMR、动画与 transition 语义，收敛到 Arrange 的阶段化 UI runtime。

运行时不以普通 custom renderer patch 作为长期中心，而以 phase-aware reactivity、Reactive Slot Runtime、typed mutation、typed slot update 与 FramePlan 为核心。

# 输入

- 编译后的 App 入口脚本与 SFC。
- Arrange host components、Modifier、基础类型与动画 API。
- 用户业务状态、组件结构状态与 UI value state。
- native 输入、资源、diagnostics、reload、HMR、VBlankSource tick 等 typed event。

# 输出

- `MutationTransaction`：结构、文本、prop、modifier、event slot 等离散界面变更。
- `SlotUpdateBatch`：layout / draw / event / resource 等 typed UI slot 更新。
- typed runtime state change。
- typed invalidation。
- FramePlan 所需输入。

# Arrange Vue runtime

Arrange Vue runtime 包含：

```txt
Composition runtime
Phase-aware reactivity
Reactive Slot Runtime
JS Value Phase scheduler
HMR / reload runtime integration
```

核心原则：

```txt
Ref 的绑定位置决定它的失效阶段。
```

普通业务和结构状态进入 Composition Phase；layout、draw、event、resource 等 UI slot 变化进入对应 slot update；高频动画值进入 JS Value Phase，不默认触发 component render 或 VNode diff。

# Reactive Slot Runtime

Reactive Slot Runtime 负责把 UI 值绑定到明确节点字段、Modifier 字段、event slot 或 resource slot：

```txt
Ref / computed / animatedXAsRef
-> typed UI slot binding
-> dirty slot batch
-> FramePlan
```

slot 必须携带：

```txt
node id / scene id
slot kind
field path
dirty role
current typed value
reason / source
```

典型 dirty role：

```txt
Layout
Draw / Paint
Transform
HitTest
Event
Resource
Accessibility
```

# VBlankSource 与动画刷新

生产视觉帧源只有 `JuceVBlankSource`。它由 JUCE `VBlankAttachment` 或等价 platform display-frame source 驱动。`VBlankReady` 是 visible UI runtime ready 的必要条件。

没有 `VBlankReady` 时，runtime 可以准备 App、pending transaction、pending slot update 与 diagnostics state，但不得推进 visual frame、动画 tick、SceneFramePipeline visual publish 或 repaint pump。

帧更新链路固定为：

```txt
VBlank tick
-> InputIntent drain
-> Composition Phase if needed
-> JS Value Phase if needed
-> FramePlan
-> Native Render Phase / SceneFramePipeline
-> PublishedFrame
-> passive paint
```

动画链路固定为：

```txt
VBlank tick
-> JS Value Phase
-> animated refs / transitions advance
-> typed slot dirty
-> SlotUpdateBatch
-> FramePlan
-> SceneFramePipeline
```

动画过程值变化不得默认触发 component render、VNode diff 或 generic patchProp。动画下一帧必须回到 VBlankSource。Promise / microtask、JS timer 或 production timer fallback 都不能承担 frame 语义。

# 运行时提交边界

`native.beginTransaction()` / `native.endTransaction()` 一类入口只表达 discrete mutation 意图。Reactive slot update 通过专门 typed slot update batch 表达。二者都不得在调用栈内直接执行 tree apply、measure、layout、draw 或 repaint。

reload、HMR reload、unmount、错误恢复和 source 切换必须清理旧 App 的 pending callbacks、pending transactions、slot bindings 与 slot update batch。

运行时不得把 JS callback、RAF callback、reload、resize、diagnostics、resource ready 或 package load 直接转成输出阶段。它们必须先进入 InputIntent、RuntimeStore、MutationTransaction 或 SlotUpdateBatch，再由 FramePlan 决定输出。

# 其余关联项

[调度线程与帧阶段](26-调度线程与帧阶段.md)；[LayoutTree与NativeTransaction](15-LayoutTree与NativeTransaction.md)；[ArrangeVue宿主目标](27-ArrangeVue宿主目标.md)；[互操作](16-互操作.md)；[事件系统](17-事件与输入.md)；[ADR 009](../adr/009-ArrangeVue阶段化响应式与ReactiveSlotRuntime.md)。



