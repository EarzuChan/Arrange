# Node.js / Vite / Arrange Vue 负责

- UI App 源码项目管理。
- SFC、template、`<script setup>` 与组件组织。
- `ref`、`reactive`、`computed`、`watch`、lifecycle 与 provide / inject authoring API。
- Arrange host target lowering：host components、props、Modifier、event slot、resource slot 与 reactive slot。
- Composition Phase 与 JS Value Phase。
- Vite dev server、HMR authoring 入口与编译期诊断。

具体 host target 见 [ArrangeVue宿主目标](27-ArrangeVue宿主目标.md)。运行时阶段见 [调度线程与帧阶段](26-调度线程与帧阶段.md)。

# Arrange core 负责

- Compose Foundation 风格布局语义。
- Modifier 编译、typed prop / slot dirty 归因与通用交互语义。
- NativeScene、LayoutTree、LayoutNode、布局、绘制、命中测试。
- InputIntent、RuntimeStore、MutationTransaction、SlotUpdateBatch、InvalidationGraph、FramePlan 与 PublishedFrame。
- QuickJS native boundary、错误屏、诊断场景与 JUCE 互操作。

# JUCE / C++ 宿主负责

- 插件与 `AudioProcessorEditor` 生命周期。
- 音频参数、宿主自动化、播放头信息。
- 平台窗口、输入事件来源、VBlankSource 来源与最终绘制 API。

# 协作原则

Arrange Vue 不直接访问 LayoutTree、DrawOpsBuilder、PublishedFrame 或 JUCE Component。JUCE 不理解 Arrange Vue 逻辑组件。JS 与 native 通过 typed mutation、typed slot update、InputIntent 与 FramePlan 协作。

用户交互以 Modifier 和 Arrange event slot 为主模型。点击、滚动、focus、hover、pointer、text input 等能力必须进入 Arrange 的命中、输入、dirty 与 FramePlan 链路。


