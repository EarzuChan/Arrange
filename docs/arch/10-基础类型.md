# 单位

```ts
dp(8)
sp(14)
px(1)
```

- `dp`：布局主单位，直接映射 JUCE 逻辑坐标。
- `sp`：文本单位，默认等于 `dp`，可叠加 Arrange 字体缩放。
- `px`：物理像素需求，主要用于 Canvas、位图和高级缓存。

# 颜色

颜色 API 对齐 Compose：

```ts
Color(0xFF000000) // 格式：AARRGGBB
Color(0x00000000) // 这样则全透明
// 只 Color(0xFFFFFF) 会导致颜色 alpha = 0
Color({ red, green, blue, alpha }) // 使用0-1的浮点值
```

主格式固定为 `0xAARRGGBB`。

`Color.Unspecified` 或等价未指定色值用于表达“调用方没有指定颜色”。它不是透明色，不应绘制为 `0x00000000`。未指定色的基础呈现由明确的 FA 契约决定，不从主题或隐式上下文取色，见 [内建 Arrangable](12-内建Arrangable.md)。

当前 Shape 支持矩形、圆和统一半径圆角；Brush 支持纯色。分角圆角、渐变属于后续设计，目前没有公开实现。

# Shape

```ts
RectangleShape
CircleShape
rounded(dp(8))
```

# Brush

```ts
solidColor(Color(0xFFFFE100))
```

`background` 最终应支持 `Color | Brush`。

# Alignment

```ts
Alignment.TopStart
Alignment.TopCenter
Alignment.TopEnd
Alignment.CenterStart
Alignment.Center
Alignment.CenterEnd
Alignment.BottomStart
Alignment.BottomCenter
Alignment.BottomEnd

Alignment.Start
Alignment.CenterHorizontally
Alignment.End

Alignment.Top
Alignment.CenterVertically
Alignment.Bottom
Alignment.Baseline
```

`BoxAlignment` 是九种二维对齐；`HorizontalAlignment` 是 Start/Center/CenterHorizontally/End，`VerticalAlignment` 是 Top/Center/CenterVertically/Bottom。Baseline 用于 Row 子项的基线对齐。`ImageAlignment` 接受二维与单轴对齐，不含 Baseline；`AlignmentValue` 是 Modifier 使用的完整集合。

`ContentScaleValue` 为 Fit/Crop/FillBounds/Inside/None/FillWidth/FillHeight。`TextAlignment` 接受 left/start/Start、center/Center、right/end/End，分别表达左、中、右对齐。

# Arrangement

```ts
Arrangement.Start
Arrangement.Center
Arrangement.End
Arrangement.Top
Arrangement.Bottom
Arrangement.SpaceBetween
Arrangement.SpaceAround
Arrangement.SpaceEvenly
Arrangement.spacedBy(dp(8))
Arrangement.spacedBy(dp(8), Alignment.Center)
```

`HorizontalArrangementProp` 使用 Start/Center/End 和三种 Space 排列；`VerticalArrangementProp` 使用 Top/Center/Bottom 和三种 Space 排列。`spacedBy` 的可选 alignment 必须属于对应轴的 Alignment，默认从主轴起点放置。

# PaddingValues

```ts
PaddingValues(dp(8))
PaddingValues({ horizontal: dp(8), vertical: dp(4) })
PaddingValues({ start, top, end, bottom })
```

# TextStyle

```ts
interface TextStyleProp {
    fontSize?: Sp
    color?: ColorValue
    lineHeight?: Sp
}
```

模板中使用 `text-style`。字体族、字重、字形与字距是尚未实现的能力，不进入当前公开类型或原生 schema。TODO：现不卡`style`了，之后可以把模板prop名字正本清源。


