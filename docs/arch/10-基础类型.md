# 单位

```ts
dp(8)
sp(14)
px(1)
```

- `dp`：布局主单位，直接映射 JUCE 逻辑坐标。
- `sp`：文本单位，默认等于 `dp`，可叠加 Arrange 字体缩放。
- `px`：物理像素需求，主要用于 Canvas、位图和高级缓存。

# 颜色

颜色 API 对齐 Compose：

```ts
Color(0xFF000000) // 格式：AARRGGBB
Color(0x00000000) // 这样则全透明
// 只 Color(0xFFFFFF) 会导致颜色 alpha = 0
Color({ red, green, blue, alpha }) // 使用0-1的浮点值
```

主格式固定为 `0xAARRGGBB`。

`Color.Unspecified` 或等价未指定色值用于表达“调用方没有指定颜色”。它不是透明色，不应绘制为 `0x00000000`。组件遇到未指定色时，应继续向 Arrange Local 或组件默认值查找。

# Shape

```ts
RectangleShape
CircleShape
rounded(dp(8))
rounded({ topStart, topEnd, bottomEnd, bottomStart })
```

# Brush

```ts
solidColor(Color(0xFFFFE100))
linearGradient([...])
radialGradient([...])
```

`background` 最终应支持 `Color | Brush`。

# Alignment

```ts
Alignment.TopStart
Alignment.TopCenter
Alignment.TopEnd
Alignment.CenterStart
Alignment.Center
Alignment.CenterEnd
Alignment.BottomStart
Alignment.BottomCenter
Alignment.BottomEnd

Alignment.Start
Alignment.CenterHorizontally
Alignment.End

Alignment.Top
Alignment.CenterVertically
Alignment.Bottom
```

# Arrangement

```ts
Arrangement.Start
Arrangement.Center
Arrangement.End
Arrangement.SpaceBetween
Arrangement.SpaceAround
Arrangement.SpaceEvenly
Arrangement.spacedBy(dp(8))
Arrangement.spacedBy(dp(8), Alignment.Center)
```

# PaddingValues

```ts
PaddingValues(dp(8))
PaddingValues({ horizontal: dp(8), vertical: dp(4) })
PaddingValues({ start, top, end, bottom })
```

# TextStyle

```ts
interface ArrangeTextStyle {
  fontSize?: Sp
  color?: ArrangeColor
  fontFamily?: string
  fontWeight?: FontWeight
  fontStyle?: FontStyle
  lineHeight?: Sp
  letterSpacing?: Sp
}
```

模板中使用 `text-style`。

